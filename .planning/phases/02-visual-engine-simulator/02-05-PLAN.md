---
phase: 02-visual-engine-simulator
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - src/engine/render.ts
  - src/client/cosmos/PhaserPainter.ts
  - src/client/cosmos/CosmosScene.ts
  - src/client/cosmos-dev/main.ts
  - src/client/cosmos-dev/cosmos-dev.html
  - src/client/cosmos-dev/cosmos-dev.css
autonomous: false
requirements: [STR-01, STR-02, QA-01]
must_haves:
  truths:
    - "Clicking a nudge (Scatter/Arms/Hue) re-synthesizes the live frontier visibly; the nudge biases the mean only, the result is still diced around it"
    - "Clicking Regenerate produces a different universe; re-entering the same seed reproduces the identical universe"
    - "The dev page has a working scrubber, nudge buttons, regenerate, seed field, and a genome-preset selector (Calm/Chaotic/Crystalline)"
  artifacts:
    - path: "src/engine/render.ts"
      provides: "Filled scrub/nudge/regenerate/destroy bodies delegating to the injected Painter (ENG-04) — no phaser import"
      contains: "nudge"
    - path: "src/client/cosmos-dev/main.ts"
      provides: "The full dev harness: sim -> render -> canvas + all controls wired"
      contains: "generateDayVectors"
    - path: "src/client/cosmos-dev/cosmos-dev.html"
      provides: "Control harness DOM: nudge buttons, regenerate, seed field, genome selector"
      contains: "Regenerate"
  key_links:
    - from: "src/client/cosmos-dev/main.ts"
      to: "src/sim/generator.ts"
      via: "Regenerate / seed field call generateDayVectors -> render.regenerate"
      pattern: "generateDayVectors"
    - from: "src/engine/render.ts"
      to: "src/engine/synthesis.ts"
      via: "nudge re-synthesizes only the frontier day (shells[0]) — biases the mean via steerGain"
      pattern: "synthesize|nudge"
---

## Phase Goal

**As a** player steering the community's universe, **I want to** nudge the live frontier (Scatter/Arms/Hue), switch genome presets, and Regenerate from a seed, **so that** I can shape and re-roll a well-told, seed-reproducible cosmos — the complete standalone demo.

<objective>
The final vertical slice that completes the standalone demo. Fill the engine `render()` handle bodies (`scrub`/`nudge`/`regenerate`/`destroy`) by delegating to the injected Painter (ENG-04). Implement **steering nudges** (Scatter/Arms/Hue) that re-synthesize ONLY the live frontier and bias the MEAN (STR-01/STR-02). Wire the **full control harness** in the dev page — nudge buttons, Regenerate (new seed), the seed field (re-entering a seed reproduces the identical universe), and the Calm/Chaotic/Crystalline genome-preset selector (QA-01) — connecting the Plan-02 simulator end-to-end. Includes a human-verify checkpoint.

Purpose: Success Criterion 3 (regenerate differs / same seed reproduces) + nudges + the complete dev page; it proves the engine complete before any Reddit code.
Output: filled `render.ts`, nudge re-synth in the painter/scene, and the complete wired dev harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-engine-simulator/02-RESEARCH.md
@.planning/phases/02-visual-engine-simulator/02-PATTERNS.md
@.planning/phases/02-visual-engine-simulator/02-UI-SPEC.md
@docs/subcosm-universe-mock.html
@.planning/phases/02-visual-engine-simulator/02-02-SUMMARY.md
@.planning/phases/02-visual-engine-simulator/02-04-SUMMARY.md

<interfaces>
<!-- The seam to fill + the simulator + presets to wire. -->

From src/engine/render.ts (fill the 4 stub bodies via the injected Painter — the Painter interface from plan 01):
```typescript
interface Painter { mount(scene, style); repaintFrontier(frontier); focus(day); remount(scene, style); destroy(); }
interface RenderHandle {
  scrub(day: number): void;                          // delegate -> painter.focus(day)  (camera, plan 04)
  nudge(param: string, amount: number): void;        // re-synth frontier -> painter.repaintFrontier
  regenerate(days: DayVector[], genome: Genome): void; // re-synth all -> painter.remount
  destroy(): void;                                   // -> painter.destroy
}
export function render(days, genome, style, painter): RenderHandle;
```

From src/engine/synthesis.ts: `synthesize(days: DayVector[], genome: Genome): Scene`. For a nudge, re-run synthesize for the frontier day only after biasing its `steering.{branch|symmetry|hue}` mean (scaled by `genome.steerGain`).

From src/sim (plan 02): `generateDayVectors(config): DayVector[]` (the parse boundary is inside it — main.ts does NOT parse again).
From src/engine/genomes: `calm`, `chaotic`, `crystalline` (the preset selector options).
From src/styles: `techno`, `crystalline` (Crystalline genome selects the crystalline style; Calm/Chaotic use techno).
</interfaces>

<nudge_reference>
<!-- STR-01/STR-02 reference: the mock's nudge (l.296-299) shifts the MEAN; the seeded RNG still dices. -->
- mock l.296: `if(f==='branch') todayBranch = min(1, todayBranch + 0.16)`
- mock l.297: `if(f==='sym') todaySym = min(6, todaySym + 1)`
- mock l.298: `if(f==='hue') todayHue = (todayHue + 24) % 360`
- then genShell(0) re-runs and the RNG dices around the new mean — STR-02 invariant: bias the mean, never dictate the outcome.
- Apply via the frontier day's `steering` + `genome.steerGain`; re-synthesize ONLY shells[0]; re-render the live frontier layer (do NOT re-bake frozen shells — RESEARCH Pattern 5).
- UI-SPEC copy: "Scatter" (❋, +wild) · "Arms" (✷, +structure) · "Hue" (◐, shift); section "Shape today's frontier".
</nudge_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill render()'s scrub/nudge/regenerate/destroy bodies and implement frontier-only nudge re-synthesis (STR-01/STR-02)</name>
  <files>src/engine/render.ts, src/client/cosmos/PhaserPainter.ts, src/client/cosmos/CosmosScene.ts</files>
  <read_first>
    - src/engine/render.ts (the 4 stub bodies throwing error.engine.render.notImplemented — fill them via the injected Painter; NO phaser import)
    - src/engine/synthesis.ts (synthesize signature — re-run for the frontier day on a nudge)
    - src/engine/contracts/Genome.ts (steerGain partialRecord(ParamEnum, number) — scales the nudge) + DayVector.ts (Steering {branch,symmetry,hue})
    - src/client/cosmos/PhaserPainter.ts (implement repaintFrontier/remount/focus/destroy here — Phaser side)
    - src/client/cosmos/CosmosScene.ts (the live frontier layer to re-render on nudge — frozen shells stay baked)
    - docs/subcosm-universe-mock.html (nudge l.296-299 — the mean-shift reference)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Pattern 5 frontier-only re-synth lines 277-281; Pitfall 2 ESLint engine boundary lines 313-317)
    - .planning/phases/02-visual-engine-simulator/02-PATTERNS.md (render.ts self-analog lines 197-200)
  </read_first>
  <action>
    In `src/engine/render.ts`, fill the four handle bodies by delegating to the injected `Painter` (the seam declared in plan 01) — `scrub(day)` -> `painter.focus(day)`; `nudge(param, amount)` -> bias the FRONTIER day's `steering[param]` mean (scaled by `genome.steerGain`), re-run `synthesize` for the frontier day only, then `painter.repaintFrontier(newFrontierShell)`; `regenerate(days, genome)` -> re-`synthesize` the whole Scene and `painter.remount(scene, style)`; `destroy()` -> `painter.destroy()`. The nudge biases the MEAN only — synthesis's seeded RNG still dices the actual element positions around it (STR-02 invariant; the outcome is never dictated). render.ts MUST stay Phaser-free (ESLint bans `phaser`/`*/client/*` in `src/engine/**`). Implement the Phaser side in `PhaserPainter.ts`: `repaintFrontier` re-renders ONLY the live frontier layer (frozen shells stay baked — do NOT re-bake them, RESEARCH Pattern 5); `remount` clears and re-bakes all shells for a new universe; `focus` drives the camera (from plan 04). Verify Phaser signatures against `node_modules/phaser` types.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm test && grep -rnE "from '(phaser)" src/engine && echo "FAIL" || echo "OK-no-phaser-in-engine"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "from '(phaser)" src/engine` returns NO matches (render.ts stays Phaser-free after filling — ENG-03/Pitfall 2).
    - `grep -c "notImplemented" src/engine/render.ts` returns 0 (all four stubs filled — ENG-04).
    - The nudge re-synthesizes the frontier and biases the mean via steerGain: `grep -cE "steerGain|steering" src/engine/render.ts` returns at least 1 AND `grep -c "synthesize" src/engine/render.ts` returns at least 1 (STR-01/STR-02).
    - `repaintFrontier` re-renders only the frontier (frozen shells not re-baked): `grep -cE "repaintFrontier|frontier" src/client/cosmos/PhaserPainter.ts` returns at least 1.
    - `npm run type-check`, `npm run lint`, and `npm test` all exit 0 (existing engine determinism tests still green — QA-02).
  </acceptance_criteria>
  <done>render()'s scrub/nudge/regenerate/destroy bodies are filled via the injected Painter with zero phaser import (ENG-04); a nudge re-synthesizes only the frontier and biases the mean while the RNG still dices (STR-01/STR-02).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the full dev-page control harness — nudge buttons, Regenerate, seed field, and the Calm/Chaotic/Crystalline preset selector</name>
  <files>src/client/cosmos-dev/main.ts, src/client/cosmos-dev/cosmos-dev.html, src/client/cosmos-dev/cosmos-dev.css</files>
  <read_first>
    - src/client/cosmos-dev/main.ts (the wiring layer to complete — sim -> render -> canvas + controls; HUD + scrubber from plan 04)
    - src/client/cosmos-dev/cosmos-dev.html + cosmos-dev.css (the chrome to add the control harness to)
    - src/sim/generator.ts (generateDayVectors(config) — Regenerate/seed call this; parse is INSIDE it, do NOT parse again in main.ts)
    - src/engine/genomes/index.ts (calm/chaotic/crystalline — the selector options) + src/styles/index.ts (techno/crystalline)
    - src/engine/render.ts (render(days, genome, style, painter) + handle.nudge/regenerate — from Task 1)
    - .planning/phases/02-visual-engine-simulator/02-UI-SPEC.md (Copywriting: "Regenerate"+"new seed", "Shape today's frontier", nudge buttons Scatter/Arms/Hue with glyphs ❋/✷/◐, "Genome" selector Calm/Chaotic/Crystalline, "Seed" field, error-state copy "That seed isn't valid — using a random one instead."; Layout control order; 44px touch targets; Color accent reserve)
    - docs/subcosm-universe-mock.html (the control markup + .fbtn faceted button treatment l.10-65)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Security V5: coerce the seed field, fall back to a random valid seed on bad input lines 489/495)
  </read_first>
  <action>
    Complete `src/client/cosmos-dev/main.ts` so the full harness is wired: on boot and on Regenerate, call `generateDayVectors({ seed })` (schema validation already happens inside the simulator — main.ts must NOT re-validate the sim output itself), then `render(days, genome, style, painter)` (or `handle.regenerate(days, genome)` for re-rolls). Wire the three nudge buttons (Scatter -> branch, Arms -> symmetry, Hue -> hue) to `handle.nudge(param, amount)`. Wire the Regenerate button to generate a NEW seed, regenerate, and reflect the new seed in the seed field. Wire the Seed field so re-entering a prior seed reproduces the IDENTICAL universe (SIM-03) — on a non-parseable seed, coerce/fall back to a random valid seed and show the UI-SPEC error copy (never a broken canvas — V5). Wire the genome-preset selector (Calm/Chaotic/Crystalline) so switching it regenerates with that genome (and Crystalline selects the crystalline style; Calm/Chaotic use techno) — proving TPL-03 visibly from the same flow. Add the control-harness DOM to `cosmos-dev.html` + style it in `cosmos-dev.css` per the UI-SPEC chrome (control order: scrub row, "Shape today's frontier" + 3-up nudge grid, Regenerate + Seed inline, Genome selector; Space Grotesk/Mono, faceted button treatment, 44px touch targets, accent reserved per UI-SPEC). Regenerate persists nothing (no confirmation dialog — there is no stored state in Phase 2).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm run build && grep -c "generateDayVectors" src/client/cosmos-dev/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "generateDayVectors" src/client/cosmos-dev/main.ts` returns at least 1 AND `grep -c ".parse(" src/client/cosmos-dev/main.ts` returns 0 (the single parse stays inside the sim — QA-03; main.ts never parses in the wiring/frame path).
    - The harness has all required controls: `grep -cE "Regenerate" src/client/cosmos-dev/cosmos-dev.html` returns at least 1 AND `grep -cE "Scatter|Arms|Hue" src/client/cosmos-dev/cosmos-dev.html` returns at least 1 AND `grep -cE "Genome|Calm|Chaotic|Crystalline" src/client/cosmos-dev/cosmos-dev.html` returns at least 1 AND a seed field exists (`grep -ci "seed" src/client/cosmos-dev/cosmos-dev.html` returns at least 1) — QA-01.
    - Nudges + regenerate are wired: `grep -cE "\\.nudge\\(|\\.regenerate\\(" src/client/cosmos-dev/main.ts` returns at least 1.
    - Bad-seed fallback present: `grep -ciE "random|isNaN|Number\\(|parseInt" src/client/cosmos-dev/main.ts` returns at least 1 (V5 coercion — never a broken canvas).
    - The whole frame path has no parse outside the sim: `grep -rc ".parse(" src/client/cosmos | grep -v test` returns 0 (QA-03).
    - `npm run type-check`, `npm run lint`, and `npm run build` exit 0.
  </acceptance_criteria>
  <done>The dev page has a working scrubber, nudge buttons, Regenerate, seed field, and Calm/Chaotic/Crystalline preset selector, all wired through the Plan-02 simulator and the engine render handle — the complete standalone demo (QA-01); the single parse boundary stays inside the sim (QA-03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify nudges, regenerate/seed reproducibility, the preset selector, and the bad-seed fallback</name>
  <files>(verification only — no files modified)</files>
  <action>Human interaction verification of the complete dev harness end-to-end. No code changes.</action>
  <what-built>Steering nudges (Scatter/Arms/Hue) that re-synthesize the live frontier and bias the mean, plus the complete dev-page control harness — scrubber, nudge buttons, Regenerate, seed field, and the Calm/Chaotic/Crystalline genome-preset selector — wired end-to-end through the simulator and engine.</what-built>
  <how-to-verify>
    1. Run the dev page (`cosmos` script). Click Scatter / Arms / Hue: confirm the LIVE frontier visibly changes within a moment (more scatter / more arms / hue shift) while the frozen inner shells do NOT change (Success Criterion: STR-01). Click the same nudge twice: the frontier shifts further but is still organically diced, not a fixed pattern (STR-02 — bias the mean, not dictate).
    2. Click Regenerate: confirm a visibly DIFFERENT but still well-told universe (cold-start -> growth -> drama -> AMA -> quiet still reads). Note the new seed shown in the Seed field.
    3. Type the PREVIOUS seed back into the Seed field and regenerate: confirm the IDENTICAL universe returns (Success Criterion 3 — same seed reproduces).
    4. Switch the Genome selector across Calm / Chaotic / Crystalline (same seed): confirm each yields a visibly different universe — denser/volatile for Chaotic, ice-blue faceted high-symmetry for Crystalline (TPL-03 + D-05), from the SAME data.
    5. Enter an invalid seed (e.g. letters): confirm the page falls back to a random valid seed with the "That seed isn't valid…" note — never a broken/blank canvas (V5).
  </how-to-verify>
  <resume-signal>Type "approved" once nudges, regenerate/seed reproducibility, the preset selector, and the bad-seed fallback all work — or describe what's off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed field → simulator | The seed text input is the only external input; it must coerce safely and never render a broken canvas |
| nudge → engine | A nudge biases the frontier steering mean and re-runs synthesis; it must never dictate the diced outcome (STR-02) |
| engine ↔ painter (render handle) | The filled handle bodies must delegate to the injected Painter without importing phaser into the engine |

> Offline interactive slice — no network/auth/persistence (Regenerate persists nothing). Threats are integrity/determinism + input-coercion hygiene.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | DoS (availability) | bad seed input crashes/garbles the canvas | mitigate | Coerce the seed to a valid integer; on non-parseable input fall back to a random valid seed + show UI-SPEC error copy (Task 2 acceptance greps the fallback) — never a broken canvas (V5) |
| T-02-14 | Tampering | render.ts pulling a phaser/client import while filling the stubs | mitigate | Bodies delegate to the injected Painter; `grep -rnE "from '(phaser)" src/engine` must return 0 (Task 1) — ENG-03/Pitfall 2 |
| T-02-15 | Tampering / determinism | nudge dictating the outcome (breaks STR-02) or a parse leaking into the frame loop (breaks QA-03) | mitigate | Nudge shifts the steering mean only; RNG dices; the single parse stays inside the sim — `grep -rc ".parse(" src/client/cosmos` (non-test) == 0 |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — all pre-installed + CI-green from Phase 1 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run type-check && npm run lint && npm test && npm run build` all green.
- Human-verify checkpoint confirms nudges (STR-01/STR-02), regenerate/seed reproducibility (SC-3), the preset selector (TPL-03/D-05), and the bad-seed fallback (V5).
- `grep -rnE "from '(phaser)" src/engine` == 0; no `.parse(` outside the sim in the frame path (QA-03).
</verification>

<success_criteria>
- Nudges re-synthesize the live frontier visibly and bias the mean only (STR-01/STR-02).
- Regenerate produces a different universe; re-entering the same seed reproduces the identical one (SC-3).
- The dev page has a working scrubber, nudge buttons, Regenerate, seed field, and Calm/Chaotic/Crystalline preset selector (QA-01).
- render()'s handle bodies are filled with zero phaser import (ENG-04); the single parse boundary stays inside the sim (QA-03).
</success_criteria>

<output>
Create `.planning/phases/02-visual-engine-simulator/02-05-SUMMARY.md` when done.
</output>
