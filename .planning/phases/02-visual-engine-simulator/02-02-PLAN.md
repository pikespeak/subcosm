---
phase: 02-visual-engine-simulator
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/sim/beats.ts
  - src/sim/generator.ts
  - src/sim/index.ts
  - src/sim/generator.test.ts
  - src/sim/beats.test.ts
autonomous: true
requirements: [SIM-01, SIM-02, SIM-03, QA-02, QA-03]
must_haves:
  truths:
    - "generateDayVectors(config) produces a realistic ~30-day scripted scenario with cold-start day-1, growth, a drama spike, an AMA day, and quiet days"
    - "The same seed reproduces an identical DayVector[]; a different seed produces a visibly different (but still well-told) universe"
    - "DayVectorSchema.parse() runs exactly once at the simulator's output boundary — never inside synthesis/paint/the frame loop"
  artifacts:
    - path: "src/sim/beats.ts"
      provides: "The scripted 30-day beat table (cold-start -> growth -> drama -> AMA -> quiet) as tunable data"
      contains: "beats"
    - path: "src/sim/generator.ts"
      provides: "generateDayVectors(config): DayVector[] with the single .parse() boundary, seeded via mulberry32"
      contains: "DayVectorSchema.parse"
    - path: "src/sim/generator.test.ts"
      provides: "SIM-02 schema-validity + SIM-03 seed-determinism tests"
      exports: []
  key_links:
    - from: "src/sim/generator.ts"
      to: "src/engine/rng.ts"
      via: "reuses mulberry32 for per-day seeds (no new PRNG, no Math.random)"
      pattern: "mulberry32"
    - from: "src/sim/generator.ts"
      to: "src/engine/contracts"
      via: "DayVectorSchema.parse() validates generated output before the engine"
      pattern: "DayVectorSchema"
---

## Phase Goal

**As a** developer demoing Subcosm, **I want to** generate a deterministic ~30-day community story (cold-start, growth, drama, AMA, quiet) from a seed, **so that** Regenerate yields a different-but-well-told universe and the same seed reproduces it exactly.

<objective>
Build the data simulator — the vertical slice that makes `Regenerate` and the seed field real. `generateDayVectors(config)` produces a scripted ~30-day `DayVector[]` (the D-03 beats), seeded deterministically from a master seed via the engine's existing `mulberry32`, with the single `DayVectorSchema.parse()` boundary at its output. TDD: the determinism + schema-validity tests are written first (RED), then the generator (GREEN).

Purpose: A judge-ready story that reads well on every regenerate (SIM-01), seed-reproducible (SIM-03), validated once at the boundary (SIM-02/QA-03).
Output: `src/sim/{beats,generator,index}.ts` + the two test files, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-engine-simulator/02-RESEARCH.md
@.planning/phases/02-visual-engine-simulator/02-PATTERNS.md

<interfaces>
<!-- The DayVector shape the generator MUST emit (validated by DayVectorSchema.parse). -->

From src/engine/contracts/DayVector.ts:
```typescript
export type Steering = { branch: number; symmetry: number; hue: number };
export type DayVector = {
  day: number;            // int positive; 1 = genesis
  date: string;           // ISO
  posts: number; comments: number; contributors: number; scoreSum: number;  // all nonnegative
  topThreads: number[];   // largest thread comment-counts -> clusters; day-1 cold-start has few/small (Pitfall 5)
  conflict: number;       // 0..1 composite
  momentum: number;       // -1..1 vs previous day
  diversity: number;      // 0..1
  dominantTheme: string;
  steering: Steering;
  seed: number;           // int
  outcome?: unknown;      // GAME-01 hook; leave undefined this phase
};
export const DayVectorSchema; // .parse() at the sim OUTPUT boundary
```

From src/engine/rng.ts (REUSE — do not write a new PRNG; no Math.random):
```typescript
export function mulberry32(seed: number): () => number;  // deterministic, byte-stable
```
Import path from src/sim/: `../engine/rng` and `../engine/contracts`.
</interfaces>

<test_pattern>
From src/engine/synthesis.test.ts (the determinism assertion shape to copy — lines 34-41):
```typescript
expect(a).toEqual(b);                              // structural deep-equal
expect(JSON.stringify(a)).toBe(JSON.stringify(b)); // byte / key-order (SIM-03)
```
Run sim tests only: `npx vitest run src/sim`. Full suite: `npm test`.
</test_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the simulator determinism, schema-validity, and beats tests against a not-yet-existing generator</name>
  <files>src/sim/generator.test.ts, src/sim/beats.test.ts</files>
  <read_first>
    - src/engine/synthesis.test.ts (copy the determinism+sensitivity test shape exactly — lines 1-50)
    - src/engine/contracts/DayVector.ts (the schema the output must satisfy)
    - .planning/phases/02-visual-engine-simulator/02-CONTEXT.md (D-03 beats: cold-start day-1 -> growth -> drama ~day 12 -> AMA ~day 20 -> quiet; seed dices WITHIN beats)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map lines 458-477; Wave 0 Gaps)
    - .planning/phases/02-visual-engine-simulator/02-PATTERNS.md (generator.test.ts + beats.test.ts assignments lines 99-108)
  </read_first>
  <behavior>
    - Test (SIM-03 determinism): generateDayVectors({ seed: 12345 }) called twice is deep-equal AND JSON.stringify-equal.
    - Test (SIM-03 sensitivity): generateDayVectors({ seed: 1 }) differs from generateDayVectors({ seed: 2 }) (the universes diverge).
    - Test (SIM-02 schema-validity): every emitted DayVector passes DayVectorSchema.parse without throwing; the generator's own output already parsed (boundary).
    - Test (SIM-01 beats): the scenario contains the required beats — day-1 cold-start (low posts, few/small topThreads, no empty-array Math.max hazard), exactly one drama-spike day (high conflict), exactly one AMA day (a few large topThreads), and quiet days; total ~30 days, all `day` values positive and ascending genesis-ward.
  </behavior>
  <action>
    Create `src/sim/generator.test.ts` and `src/sim/beats.test.ts` importing the not-yet-existing `generateDayVectors` from `./generator` and the beats table from `./beats`. Encode the four behaviors above. For SIM-03 use BOTH `toEqual` and `JSON.stringify` equality (copy synthesis.test.ts). For SIM-01 assert structural beat properties (a drama day with `conflict` above a high threshold; an AMA day whose `topThreads` has a few large values; a cold-start day-1 with small/empty-safe `topThreads`) — assert on the DATA, not on internal beat-table indices, so the test survives Claude's-discretion tuning of beat positions. These tests MUST fail to import/run now (RED). Do NOT implement the generator in this task.
  </action>
  <verify>
    <automated>npx vitest run src/sim 2>&1 | grep -qE "fail|error|cannot find|Cannot find" && echo "RED-OK" || (echo "EXPECTED RED FAILURE" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `src/sim/generator.test.ts` exists and references `generateDayVectors` and `DayVectorSchema`.
    - `src/sim/beats.test.ts` exists and asserts the presence of cold-start / drama / AMA / quiet beats on the OUTPUT data.
    - `npx vitest run src/sim` FAILS (RED) — the generator/beats modules do not exist yet.
    - `grep -c "JSON.stringify" src/sim/generator.test.ts` returns at least 1 (byte-level determinism assertion, SIM-03).
  </acceptance_criteria>
  <done>The simulator tests exist and fail for the right reason (no implementation) — SIM-01/02/03 expectations are pinned before any generator code is written.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the beats table and generateDayVectors with the single DayVectorSchema.parse() boundary</name>
  <files>src/sim/beats.ts, src/sim/generator.ts, src/sim/index.ts</files>
  <read_first>
    - src/sim/generator.test.ts + src/sim/beats.test.ts (the contract just written — make them pass)
    - src/engine/genomes/calm.ts (the data-module + module-load XSchema.parse boundary convention to mirror for beats + the parse-at-output discipline)
    - src/engine/rng.ts (mulberry32 — the ONLY entropy source; reuse, never re-implement)
    - src/engine/contracts/DayVector.ts (the exact output shape + nonnegative/range constraints)
    - .planning/phases/02-visual-engine-simulator/02-CONTEXT.md (D-03 story beats + "seed only dices WITHIN beats")
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Pitfall 5 cold-start day-1 lines 331-335; Open Question 1 beat-table-as-data lines 416-419)
    - .planning/phases/02-visual-engine-simulator/02-PATTERNS.md (generator.ts + beats.ts assignments lines 71-95)
  </read_first>
  <action>
    Create `src/sim/beats.ts` as a tunable DATA table (mirror `calm.ts`'s header-comment-then-data convention) describing the ~30-day arc: cold-start day-1, a growth ramp, one drama spike (~day 12 — high conflict / red turbulence), one AMA day (~day 20 — a few huge `topThreads`), and quiet days. Exact day count and beat day-indices are Claude's discretion within this shape. Create `src/sim/generator.ts` exporting `generateDayVectors(config)`: derive per-day seeds from `config.seed` via `mulberry32` (reuse `../engine/rng`; NO `Math.random`), build each day's counted/derived fields by dicing WITHIN its beat (the seed shifts values around the beat's mean — a regenerate stays well-told), guard the cold-start day so `topThreads` is small but never an empty array spread into `Math.max` (Pitfall 5), set `steering` to neutral defaults, and set `outcome` undefined. Apply `DayVectorSchema.parse()` to each generated day at the OUTPUT — the SINGLE parse boundary for this phase (SIM-02/QA-03); it must appear ONLY here, never in synthesis/paint/the frame loop. Add `src/sim/index.ts` barrel re-exporting `generateDayVectors` + the beats table. Make all tests from Task 1 pass.
  </action>
  <verify>
    <automated>npx vitest run src/sim && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/sim` PASSES (all SIM-01/02/03 tests green).
    - `grep -c "DayVectorSchema.parse" src/sim/generator.ts` returns 1 — the single output-boundary parse (SIM-02). The whole `src/sim` tree has exactly one parse site: `grep -rc "DayVectorSchema.parse" src/sim | grep -v test` shows the only non-test occurrence is in generator.ts.
    - `grep -c "mulberry32" src/sim/generator.ts` returns at least 1 AND `grep -rcE "Math\\.random" src/sim` returns 0 (deterministic, reused PRNG — SIM-03).
    - `npm run type-check` and `npm run lint` both exit 0 (the new eslint block bans phaser in src/sim and passes).
    - `npm test` (full suite) stays green — existing engine tests unaffected (QA-02).
  </acceptance_criteria>
  <done>generateDayVectors produces the scripted seed-deterministic 30-day story, validated once at its output boundary via DayVectorSchema.parse(), reusing mulberry32 with no Math.random — SIM-01/02/03 tests pass and the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator → engine | Generated DayVector[] crosses into synthesis; the single validation point is DayVectorSchema.parse() at the sim output |
| seed input → simulator | A numeric master seed is the only external input that drives generation |

> Offline data-generation slice — NO network, NO persistence. Threats are integrity/determinism, not web attacks.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | malformed sim output reaching the engine | mitigate | DayVectorSchema.parse() at the sim output boundary (SIM-02) surfaces a structured error instead of a silent NaN; Task 2 acceptance greps for exactly one parse site |
| T-02-05 | Tampering / determinism | non-determinism leaking into generation (Math.random / unstable order) breaks SIM-03 | mitigate | Reuse mulberry32 only; `grep -rcE "Math\\.random" src/sim` must return 0; SIM-03 test asserts byte-identical output for the same seed |
| T-02-06 | DoS (availability) | cold-start day-1 empty topThreads spread into Math.max -> NaN/garbled day | mitigate | Pitfall-5 guard: day-1 emits a valid small-but-nonempty-safe topThreads; the SIM-01 beats test covers cold-start validity |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — zod + vitest + the engine rng are all pre-installed and CI-green from Phase 1 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run src/sim` passes (SIM-01 beats, SIM-02 schema-validity, SIM-03 determinism + sensitivity).
- `npm test && npm run type-check && npm run lint` all green.
- Single parse boundary confirmed: only one non-test `DayVectorSchema.parse` in `src/sim`, none anywhere in synthesis/paint (QA-03).
</verification>

<success_criteria>
- `generateDayVectors(config)` yields a realistic scripted ~30-day scenario (cold-start, growth, drama, AMA, quiet) — SIM-01.
- Same seed -> byte-identical `DayVector[]`; different seed -> a different well-told universe — SIM-03.
- Exactly one `DayVectorSchema.parse()` at the output boundary, no `Math.random`, full suite green — SIM-02/QA-02/QA-03.
</success_criteria>

<output>
Create `.planning/phases/02-visual-engine-simulator/02-02-SUMMARY.md` when done.
</output>
