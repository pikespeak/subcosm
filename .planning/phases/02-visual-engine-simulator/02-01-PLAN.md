---
phase: 02-visual-engine-simulator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/tsconfig.styles.json
  - tools/tsconfig.sim.json
  - tsconfig.json
  - eslint.config.js
  - vite.config.ts
  - package.json
  - src/styles/techno.ts
  - src/styles/index.ts
  - src/engine/render.ts
  - src/client/cosmos/CosmosScene.ts
  - src/client/cosmos/PhaserPainter.ts
  - src/client/cosmos/primitives.ts
  - src/client/cosmos/bake.ts
  - src/client/cosmos-dev/cosmos-dev.html
  - src/client/cosmos-dev/main.ts
  - src/client/cosmos-dev/cosmos-dev.css
autonomous: true
requirements: [PNT-01, PNT-02, PNT-03, QA-01, QA-02, QA-03]
must_haves:
  truths:
    - "A standalone Vite dev page boots a Phaser WebGL canvas separate from the Devvit game.html build"
    - "A single synthesized Scene paints to the canvas — genesis core + at least the shell-0 stars are visible end-to-end"
    - "The engine never imports phaser; the Painter seam is injected from src/client/cosmos/"
  artifacts:
    - path: "src/client/cosmos-dev/cosmos-dev.html"
      provides: "Standalone Vite entry for the dev harness (not a devvit.json entrypoint)"
    - path: "src/client/cosmos/PhaserPainter.ts"
      provides: "Phaser implementation of the engine Painter interface"
      min_lines: 30
    - path: "src/styles/techno.ts"
      provides: "Techno StyleTemplate data object (schema-parsed at module load)"
      contains: "StyleTemplateSchema.parse"
    - path: "src/engine/render.ts"
      provides: "render() with a Painter seam (interface declared, no phaser import)"
      contains: "Painter"
  key_links:
    - from: "src/client/cosmos-dev/main.ts"
      to: "src/engine/render.ts"
      via: "render(days, genome, style, painter) wires sim output through the engine into PhaserPainter"
      pattern: "render\\("
    - from: "src/client/cosmos/PhaserPainter.ts"
      to: "src/styles/techno.ts"
      via: "paint reads StyleTemplate (techno) to map hue->color"
      pattern: "techno|StyleTemplate"
---

## Phase Goal

**As a** developer proving the engine, **I want to** open a standalone dev page that renders a synthesized Subcosm Scene on a Phaser WebGL canvas, **so that** I can see the universe paint end-to-end before any Reddit code exists.

<objective>
Establish the Phase-2 build surface (new `src/styles/` and `src/sim/` project coverage, a separate Vite dev entry) and deliver the **thinnest end-to-end render slice**: a fixture Scene synthesized through the existing engine, painted to a Phaser canvas via an injected `Painter`, with the genesis core and shell-0 stars visible. This is the MVP happy path — every later plan refines this surface.

Purpose: Prove the `Scene` -> Phaser seam works (PNT-01 foundation) with the engine staying Phaser-free, and unblock all downstream paint/camera/steering plans by standing up the dev page and config (QA-01).
Output: New build-config coverage, the `techno` StyleTemplate, the `Painter` seam in `render.ts`, the Phaser scene + painter + glow/bake primitives, and the standalone `cosmos-dev.html` dev page booting a visible canvas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-engine-simulator/02-RESEARCH.md
@.planning/phases/02-visual-engine-simulator/02-PATTERNS.md
@.planning/phases/02-visual-engine-simulator/02-UI-SPEC.md
@docs/subcosm-universe-mock.html

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/engine/. Use directly — no exploration needed. -->

From src/engine/render.ts (the stub to fill — declare Painter here, types-only, NO phaser import):
```typescript
export interface RenderHandle {
  readonly scene: Scene;
  readonly style: StyleTemplate;
  scrub(day: number): void;        // Phase 2 — camera focus (filled in plan 04)
  nudge(param: string, amount: number): void;  // Phase 2 — plan 05
  regenerate(days: DayVector[], genome: Genome): void;  // Phase 2 — plan 05
  destroy(): void;
}
export function render(days: DayVector[], genome: Genome, style: StyleTemplate): RenderHandle;
```

From src/engine/contracts/Scene.ts (what paint reads — geometry only; hue is a 0..1 HINT, never a color):
```typescript
export type CoreNode = { radius: number; energy: number; hue: number };      // hue 0..1
export type Element = { kind: 'star'|'cluster'|'filament'; angle: number; r: number; size: number; energy: number; hue: number; conflict: number; big: boolean };
export type ShellMeta = { date: string; era: string; theme: string; posts: number; comments: number; contributors: number; conflict: number };
export type Shell = { day: number; radius: number; meta: ShellMeta; elements: Element[] };  // radius already = pow(0.85, idx)
export type Scene = { core: CoreNode; shells: Shell[]; goalAchieved: boolean | null };
```

From src/engine/contracts/StyleTemplate.ts (the data object to author — id MUST be 'techno', genes is Record<string,string>):
```typescript
export type StyleTemplate = {
  id: 'techno'|'comic'|'pixel'; substrate: string;
  palette: { space: string; ramp: string[] };
  line: { width: number; join: string; alpha: number };
  fill: { mode: string; alpha: number };
  texture: { kind: string; scale: number };
  genes: Record<string, string>;           // Gene -> PrimitiveRef
  postFX: { bloom: number; grain: number };
  motion: { frontierOnly: boolean; speed: number };  // drives PNT-04 reduced-motion (plan 03)
  type: { family: string; weight: number };
};
```

From src/engine/contracts (barrel) — import path: `../engine/contracts` (styles) / `../../engine/contracts` (cosmos):
```typescript
export { SceneSchema, StyleTemplateSchema, DayVectorSchema, GenomeSchema, type Scene, type StyleTemplate, type DayVector, type Genome };
```
From src/engine/genomes: `export { calm, chaotic, crystalline }`.
From src/engine/render.ts: `export { render, type RenderHandle }`.
</interfaces>

<build_topology>
<!-- How Phase 1 added a project — mirror this for src/styles and src/sim. -->
- `tools/tsconfig.engine.json`: extends `./tsconfig.base.json`, sets `outDir`/`tsBuildInfoFile` under `../dist/types/<area>`, `rootDir: "../src/engine"`, `include: ["../src/engine/**/*"]`.
- `tsconfig.json`: `references` array lists every `tools/tsconfig.*.json`.
- `eslint.config.js`: the engine block (after the catch-all) sets `files: ['src/engine/**/*.{ts,tsx}']`, `parserOptions.project: ['./tools/tsconfig.engine.json', ...]`, and `no-restricted-imports` patterns `['@devvit/*','phaser','*/client/*','*/server/*']`.
- `src/client/**` is already covered by `tools/tsconfig.client.json` + the client eslint block → `src/client/cosmos/` and `src/client/cosmos-dev/` need NO new tsconfig (they fall under `src/client/**`).
- `vite.config.ts` is wrapped by `@devvit/start/vite`; `devvit.json` declares splash.html + game.html. The dev page must NOT enter that bundle (Pitfall 1).
</build_topology>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up build coverage for the new src/styles + src/sim dirs and a separate dev-page Vite entry</name>
  <files>tools/tsconfig.styles.json, tools/tsconfig.sim.json, tsconfig.json, eslint.config.js, vite.config.ts, package.json</files>
  <read_first>
    - tools/tsconfig.engine.json (the per-area project template to copy)
    - tools/tsconfig.base.json (the extended base; note composite + strict)
    - tsconfig.json (the references array to extend)
    - eslint.config.js (the engine-boundary block lines 70-113 — the no-restricted-imports pattern to mirror)
    - vite.config.ts (the devvit-wrapped config; Pitfall 1 RESEARCH lines 307-311)
    - package.json (scripts: build/lint/type-check/test)
    - .planning/phases/02-visual-engine-simulator/02-PATTERNS.md ("New-dir build coverage" + config table lines 36-43)
  </read_first>
  <action>
    Create `tools/tsconfig.styles.json` and `tools/tsconfig.sim.json`, each extending `./tsconfig.base.json`, with `rootDir` `../src/styles` / `../src/sim`, matching `outDir`/`tsBuildInfoFile` under `../dist/types/{styles,sim}`, and `include` for their dir. These projects allow `zod` + engine contract type imports but MUST NOT allow `phaser`. Add both to the `references` array in `tsconfig.json`. In `eslint.config.js`, add ONE new flat-config block (placed after the trailing catch-all, like the engine block) with `files: ['src/styles/**/*.{ts,tsx}', 'src/sim/**/*.{ts,tsx}']`, `parserOptions.project` pointing at the two new tsconfigs, and `no-restricted-imports` banning `['@devvit/*','phaser','*/client/*','*/server/*']` (mirror the engine block; this lint-enforces "styles/sim are Phaser-free, parse-at-boundary"). For the dev-page entry: add `cosmos-dev.html` as a SEPARATE plain-vite entry that the Devvit build ignores — per Pitfall 1 ([ASSUMED] cleanest path is a standalone vite invocation, not a `devvit.json` entrypoint). Add a `cosmos` script to `package.json` (e.g. `vite --config <dev-only config or root> src/client/cosmos-dev` or a `rollupOptions.input` addition guarded so `npm run build` for Devvit stays green). Verify the exact mechanism against current `@devvit/start` behavior at implement time. Do NOT add `cosmos-dev.html` to `devvit.json` entrypoints. `src/client/cosmos*` needs no new tsconfig (covered by `src/client/**`).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0 with `tools/tsconfig.styles.json` and `tools/tsconfig.sim.json` present in `tsconfig.json` references.
    - `npm run lint` exits 0 and the new eslint block covers `src/styles/**` + `src/sim/**`: `grep -nE "src/(styles|sim)" eslint.config.js` returns at least one match and a `no-restricted-imports` entry banning `phaser` applies to those globs.
    - `npm run build` (the Devvit build) exits 0 and does NOT emit `cosmos-dev.html` into `dist/client`: `grep -rc "cosmos-dev" devvit.json` returns 0 (the dev entry is not a Devvit entrypoint).
    - A `cosmos` (or equivalently named) script exists in `package.json` to run the dev page via plain vite.
  </acceptance_criteria>
  <done>New tsconfig projects + eslint block cover src/styles and src/sim with Phaser banned; the dev page is a separate vite entry kept out of the Devvit bundle; type-check, lint, and Devvit build all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the Techno StyleTemplate, add the Painter seam to render.ts, and build the glow/bake Phaser primitives</name>
  <files>src/styles/techno.ts, src/styles/index.ts, src/engine/render.ts, src/client/cosmos/primitives.ts, src/client/cosmos/bake.ts</files>
  <read_first>
    - src/engine/genomes/calm.ts (the exact data-module shape + header-comment convention + module-load XSchema.parse boundary to copy)
    - src/engine/contracts/StyleTemplate.ts (the StyleTemplateSchema field set the techno object must satisfy)
    - src/engine/render.ts (the stub to modify — declare Painter here, types-only)
    - src/engine/contracts/Scene.ts (Scene/Shell/Element/CoreNode shapes paint consumes; hue is 0..1)
    - src/client/scenes/Game.ts (Phaser API idioms: this.add.image, setBlendMode, setTint, setScale, scene lifecycle)
    - docs/subcosm-universe-mock.html (glow gradients at lines 219/239/258; vignette 267; the LOOK to reproduce — NOT the canvas calls)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Phaser<->Canvas mapping table lines 184-198; Pattern 3 glow texture lines 266-270; Pattern 2 bake lines 253-264; Code Examples lines 346-367; Pitfall 4 lines 325-329)
    - .planning/phases/02-visual-engine-simulator/02-UI-SPEC.md (Color section — the techno palette the chrome harmonizes with: cyan #46e0d8 / magenta nebula / warm-white #fff7e6 core)
  </read_first>
  <behavior>
    - techno StyleTemplate parses cleanly: importing src/styles/techno.ts does not throw (StyleTemplateSchema.parse succeeds), id === 'techno', palette.ramp has the cyan<->magenta + warm-white stops, motion.frontierOnly === true.
    - render() exposes a Painter seam: render.ts declares a Painter interface (types only) and no phaser/client import path reaches src/engine/ (eslint passes).
  </behavior>
  <action>
    Create `src/styles/techno.ts` exporting `export const techno: StyleTemplate = StyleTemplateSchema.parse({ ... })`, copying `calm.ts`'s header-comment-then-data convention and importing `StyleTemplateSchema` from `../engine/contracts`. Set `id: 'techno'`, a `palette.ramp` carrying the Techno look (deep space indigo -> cyan `#46e0d8` -> magenta -> warm-white `#fff7e6` per UI-SPEC), `motion: { frontierOnly: true, speed: ... }` (consumed by reduced-motion in plan 03), `genes` mapping star/cluster/filament gene refs to primitive names the painter understands, and the remaining schema fields as sensible Techno data (NOT hard-coded in paint — PNT-02). Add `src/styles/index.ts` barrel. In `src/engine/render.ts`, declare a `Painter` interface (types only — `mount(scene, style)`, `repaintFrontier(frontier)`, `focus(day)`, `remount(scene, style)`, `destroy()`) per RESEARCH Pattern 1; wire `render()` to accept/hold a Painter so `scrub/nudge/regenerate/destroy` can delegate to it in later plans — but DO NOT import phaser or anything under `*/client/*` (ESLint will fail otherwise). Create `src/client/cosmos/primitives.ts` generating ONE reused soft radial-glow texture at boot (`this.make.graphics` -> `generateTexture('glow', ...)`, or a one-time CanvasTexture for true falloff per Pitfall 4 / Assumption A3) plus a star/core primitive; every glow draw is a tinted+scaled additive `Image` (`setBlendMode(Phaser.BlendModes.ADD)`) — never a per-frame gradient. Create `src/client/cosmos/bake.ts` with a helper that draws a shell's additive Images into a `DynamicTexture` (`this.textures.addDynamicTexture` -> `dt.draw(container)`) and composites it as one `Image` (PNT-03 bake-on-freeze). Verify Phaser 4.2 method signatures against `node_modules/phaser` types (Assumption A2) — do not trust rc.6 names blindly.
  </action>
  <verify>
    <automated>npm run lint && npm run type-check && grep -rnE "from '(phaser)" src/engine && echo "FAIL: phaser in engine" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "from '(phaser)" src/engine` returns NO matches (engine stays Phaser-free; QA-03/ENG-03 boundary).
    - `grep -c "StyleTemplateSchema.parse" src/styles/techno.ts` returns 1 (module-load parse boundary, PNT-02).
    - `grep -c "Painter" src/engine/render.ts` returns at least 1 (the seam interface is declared in the engine, types only).
    - `grep -c "BlendModes.ADD" src/client/cosmos/primitives.ts` returns at least 1 (additive glow == mock 'lighter').
    - `grep -c "addDynamicTexture" src/client/cosmos/bake.ts` returns at least 1 (DynamicTexture bake helper present, PNT-03).
    - `npm run type-check` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <done>The techno StyleTemplate is schema-parsed data (PNT-02); render.ts has a Painter seam with zero phaser import; the reused glow texture (additive) and DynamicTexture bake helper exist as the perf foundation (PNT-03); all checks green.</done>
</task>

<task type="auto">
  <name>Task 3: Boot the standalone dev page and paint one synthesized Scene end-to-end on the Phaser canvas</name>
  <files>src/client/cosmos/CosmosScene.ts, src/client/cosmos/PhaserPainter.ts, src/client/cosmos-dev/cosmos-dev.html, src/client/cosmos-dev/main.ts, src/client/cosmos-dev/cosmos-dev.css</files>
  <read_first>
    - src/client/game.ts (the Phaser boot pattern: GameConfig type: AUTO, Phaser.Scale.RESIZE, scene array, new Game inside DOMContentLoaded)
    - src/client/game.html (the HTML entry structure: game-container div, module script, css link)
    - src/client/game.css (file shape for cosmos-dev.css)
    - src/client/scenes/Game.ts (Phaser.Scene class + this.cameras.main + scale resize idioms)
    - src/engine/render.ts (render(days, genome, style) entry + RenderHandle — now with Painter seam from Task 2)
    - src/engine/genomes/calm.ts (a real Genome preset to feed render for this slice)
    - tests/fixtures.ts (fixtureDays — a ready DayVector[] to render before the simulator exists in plan 02)
    - .planning/phases/02-visual-engine-simulator/02-PATTERNS.md (cosmos-dev/main.ts + CosmosScene assignments lines 112-131, 175-183)
    - .planning/phases/02-visual-engine-simulator/02-UI-SPEC.md (Layout: full-bleed canvas stage, touch-action:none; the canvas container)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (System Architecture diagram lines 125-178; DPR cap mock line 173 / PNT-03)
  </read_first>
  <action>
    Create `src/client/cosmos/CosmosScene.ts` extending `Phaser.Scene` (copy the class/lifecycle skeleton from `scenes/Game.ts`): in `create()` grab `this.cameras.main`, generate the reused glow texture (primitives.ts), and paint a passed-in `Scene` — draw the genesis core (large additive warm-white glow Image + bright core dot per the mapping table) and the shell-0 stars (tinted additive glow Images at `element.angle`/`element.r` scaled by `element.size`, hue resolved through `techno.palette.ramp`). Bake non-frontier shells via `bake.ts` (acceptable to start with just core+shell-0 visible for THIS slice; fuller parity lands in plan 03). Create `src/client/cosmos/PhaserPainter.ts` implementing the engine `Painter` interface — `mount(scene, style)` instantiates/feeds the CosmosScene; other methods may be minimal stubs for now (filled in plans 04/05). Create `src/client/cosmos-dev/cosmos-dev.html` (copy `game.html` structure; add a `#cosmos-stage` canvas container with `touch-action: none`), `src/client/cosmos-dev/cosmos-dev.css` (minimal full-bleed dark stage per UI-SPEC; full chrome lands in plan 05), and `src/client/cosmos-dev/main.ts` that: imports `fixtureDays` + `calm` + `techno`, calls `render(fixtureDays, calm, techno, painter)` (constructing `PhaserPainter`), boots Phaser (`type: AUTO`, `Phaser.Scale.RESIZE`, DPR capped at 2 per PNT-03) inside `DOMContentLoaded`, and mounts the resulting Scene to the canvas. Cap DPR at 2 in the game config (mock line 173). The Scene MUST come from the engine `render()` output — main.ts must NOT call `synthesize` directly (render is the ENG-04 orchestration entry).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "render(" src/client/cosmos-dev/main.ts` returns at least 1 AND `grep -c "synthesize" src/client/cosmos-dev/main.ts` returns 0 (the dev page goes through the engine render() entry, not synthesis directly — ENG-04).
    - `grep -c "type: AUTO" src/client/cosmos-dev/main.ts` returns 1 (WebGL-preferred Phaser boot, PNT-01).
    - `grep -cE "devicePixelRatio|resolution|Math.min.*2" src/client/cosmos-dev/main.ts` returns at least 1 (DPR cap, PNT-03).
    - `grep -c "touch-action" src/client/cosmos-dev/cosmos-dev.css` returns at least 1 (pinch drives camera not page, UI-SPEC).
    - `npm run type-check`, `npm run lint`, and `npm run build` (Devvit) all exit 0 with the new files present and the Devvit bundle still green.
  </acceptance_criteria>
  <done>The dev page boots a Phaser WebGL canvas via a separate Vite entry, runs a fixture DayVector[] through the engine render() entry, and paints the genesis core + shell-0 stars visibly — the thinnest end-to-end Scene->pixels slice (PNT-01 foundation, QA-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine ↔ paint (Scene seam) | Geometry-only Scene crosses into the Phaser painter; paint must never reach back for raw DayVector |
| build config ↔ Devvit bundle | The dev-page Vite entry must not leak into the shipped Devvit `dist/client` |

> Phase 2 is an offline standalone dev page — NO network, NO auth, NO persistence, NO Devvit runtime. Realistic threats are integrity/determinism, not web attacks.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | engine boundary (render.ts importing phaser) | mitigate | Painter declared types-only in engine; ESLint `no-restricted-imports` bans `phaser`/`*/client/*` in `src/engine/**` (Task 2 acceptance greps for 0 phaser imports) |
| T-02-02 | Tampering | Devvit build picking up the dev page | mitigate | `cosmos-dev.html` is a separate plain-vite entry, NOT a `devvit.json` entrypoint; Task 1 verifies `npm run build` stays green and grep shows 0 cosmos-dev refs in devvit.json |
| T-02-03 | Information disclosure | XSS via strings rendered in DOM | accept | No user input and no HUD text in this plan; chrome text lands in plan 05 via textContent (deferred) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit) — all four core packages pre-installed + CI-green from Phase 1 |
</threat_model>

<verification>
- `npm run type-check && npm run lint && npm test && npm run build` all green (existing engine tests must still pass).
- `grep -rnE "from '(phaser)" src/engine` returns 0 (engine Phaser-free).
- The dev page runs via the new `cosmos` script and shows a glowing genesis core + shell-0 stars on a WebGL canvas (manual visual check — no headless WebGL assertion in scope per RESEARCH Validation Architecture).
</verification>

<success_criteria>
- New build coverage for `src/styles/` + `src/sim/` (Phaser banned) and a separate dev-page Vite entry exist; the Devvit build is unaffected.
- A fixture Scene synthesized through the engine `render()` paints to a Phaser canvas end-to-end — genesis core + shell-0 stars visible.
- The engine imports no Phaser; the Painter seam is injected from `src/client/cosmos/`.
- The reused additive glow texture and DynamicTexture bake helper exist as the PNT-03 perf foundation.
</success_criteria>

<output>
Create `.planning/phases/02-visual-engine-simulator/02-01-SUMMARY.md` when done.
</output>
