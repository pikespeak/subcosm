---
phase: 02-visual-engine-simulator
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/client/cosmos/paint.ts
  - src/client/cosmos/CosmosScene.ts
  - src/client/cosmos/reduced-motion.ts
  - src/styles/crystalline.ts
  - src/styles/index.ts
autonomous: false
requirements: [PNT-01, PNT-02, PNT-03, PNT-04]
must_haves:
  truths:
    - "The dev page renders a universe whose genesis core, concentric shells, nebula, frontier ignite, and vignette match docs/subcosm-universe-mock.html side-by-side"
    - "Switching the Crystalline genome shows an ice-blue, faceted, high-symmetry look clearly distinct from Calm/Chaotic Techno"
    - "prefers-reduced-motion produces a static, non-strobe frame across the whole canvas"
  artifacts:
    - path: "src/client/cosmos/paint.ts"
      provides: "Scene+StyleTemplate -> Phaser objects: nebula, stars, frontier ignite, genesis core, vignette per the mock-mapping table"
      min_lines: 60
    - path: "src/styles/crystalline.ts"
      provides: "Crystalline techno-id StyleTemplate (ice-blue palette + faceted/angular primitive) — no new StyleId, no contract change"
      contains: "StyleTemplateSchema.parse"
    - path: "src/client/cosmos/reduced-motion.ts"
      provides: "Static-frame branch honoring prefers-reduced-motion (PNT-04)"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/client/cosmos/paint.ts"
      to: "src/styles/techno.ts"
      via: "paint reads all look constants from StyleTemplate (hue 0..1 -> palette.ramp color), never hard-codes them"
      pattern: "palette|style\\."
    - from: "src/client/cosmos/reduced-motion.ts"
      to: "src/styles/techno.ts"
      via: "reads StyleTemplate.motion.frontierOnly/speed to drive the static branch"
      pattern: "motion"
---

## Phase Goal

**As a** judge viewing the demo, **I want to** see a universe that visually matches the mock (core, shells, nebula, frontier ignite, vignette) and a clearly distinct Crystalline look, **so that** Subcosm reads as a polished, self-authored, accessible universe — even with reduced motion.

<objective>
Bring the canvas to full **mock parity** (PNT-01) — building on plan 01's glow/bake foundation, paint the per-shell nebula clouds, the small + big stars, the pulsing frontier ignite ring, the genesis core, and the vignette, all from `StyleTemplate` data (PNT-02). Author the **Crystalline** StyleTemplate (ice-blue/faceted, D-05) as a techno-id instance with no contract change. Honor **prefers-reduced-motion** with a static, non-strobe frame across the whole surface (PNT-04). Includes a human-verify checkpoint for the visual-parity bar.

Purpose: This is the "side-by-side with the mock is convincing" success criterion (SC-1) plus the distinct-Crystalline look and the accessibility hard rule.
Output: `paint.ts` (full draw), `reduced-motion.ts`, `crystalline.ts`, and the CosmosScene frame loop animating only the frontier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-engine-simulator/02-RESEARCH.md
@.planning/phases/02-visual-engine-simulator/02-PATTERNS.md
@.planning/phases/02-visual-engine-simulator/02-UI-SPEC.md
@docs/subcosm-universe-mock.html
@.planning/phases/02-visual-engine-simulator/02-01-SUMMARY.md

<interfaces>
<!-- From plan 01 (already built): the painter seam + primitives + bake. -->
- `src/client/cosmos/primitives.ts`: the reused additive glow texture + star/core primitive generators.
- `src/client/cosmos/bake.ts`: `addDynamicTexture` bake helper — frozen shells composited as one Image.
- `src/client/cosmos/CosmosScene.ts`: the Phaser.Scene drawing core + shell-0 (extend its draw + frame loop here).
- `src/styles/techno.ts`: `export const techno: StyleTemplate` — read all look constants from here.

From src/engine/contracts/Scene.ts — paint reads geometry only:
```typescript
type Element = { kind: 'star'|'cluster'|'filament'; angle: number; r: number; size: number; energy: number; hue: number; conflict: number; big: boolean };
type Shell = { day: number; radius: number; meta: ShellMeta; elements: Element[] };  // shells[0] = frontier (live)
type CoreNode = { radius: number; energy: number; hue: number };
type Scene = { core: CoreNode; shells: Shell[]; goalAchieved: boolean | null };
```
From src/engine/contracts/StyleTemplate.ts: `StyleTemplate.motion = { frontierOnly: boolean; speed: number }`, `genes: Record<string,string>`, `palette.ramp: string[]`.
From src/engine/genomes/crystalline.ts: the Crystalline GENOME already exists (palette ramp `['#04121a','#0e4d5e','#46e0d8','#e8fbff']`, `allowedGenes: ['facet','spike','aura']`, symmetry 5) — author the matching STYLE here.
</interfaces>

<mock_mapping>
<!-- The authoritative Canvas->Phaser translation (RESEARCH lines 184-198). Translate behavior, never copy canvas calls. -->
| Mock (subcosm-universe-mock.html) | Phaser 4.2 |
|---|---|
| `globalCompositeOperation='lighter'` (l.195) | `setBlendMode(Phaser.BlendModes.ADD)` |
| `createRadialGradient` star/nebula/core (l.219/239/258) | the ONE reused glow texture (primitives.ts), `setTint(hue->palette)` + `setScale` |
| 90 bg stars (l.196) | baked once (DynamicTexture), never re-rendered |
| orbital guide rings (l.199-203) | `Graphics.strokeCircle`, baked, low alpha |
| per-shell nebula clouds (l.213-223) | additive glow Images, tinted by shell hue, scaled to radius — baked (frozen) or live (frontier) |
| stars (l.228-246): small dot + big glow | small = tiny additive dot; big = glow Image + bright core dot; Crystalline swaps the dot for an angular/faceted primitive |
| frontier ignite ring (l.249-252, pulsing) | `Graphics.strokeCircle` redrawn per frame w/ animated alpha/lineWidth — frontier only |
| genesis core (l.256-263) | large additive warm-white glow Image + bright core dot (always live, cheap) |
| vignette (l.267-269) | full-screen dark radial-alpha texture on top (BlendModes.NORMAL) |
| reduced branch (l.112,163,186,250,257) | `reduce` => `zoom=target; intro=1; rot=0; ignite=1; tw=1`, stop animating |
</mock_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Crystalline StyleTemplate (ice-blue, faceted) and complete the mock-parity paint draw</name>
  <files>src/styles/crystalline.ts, src/styles/index.ts, src/client/cosmos/paint.ts</files>
  <read_first>
    - src/styles/techno.ts (the sibling StyleTemplate to mirror; Crystalline = same id 'techno', different palette/genes)
    - src/engine/genomes/crystalline.ts (the matching genome: palette ramp, allowedGenes facet/spike/aura, symmetry 5)
    - src/engine/contracts/StyleTemplate.ts (StyleTemplateSchema field set + StyleIdEnum is ['techno','comic','pixel'] — Crystalline is NOT a new id)
    - src/client/cosmos/primitives.ts (reused glow texture + how to add a faceted/angular star-polygon primitive)
    - src/client/cosmos/paint.ts OR CosmosScene.ts (the plan-01 draw to extend)
    - docs/subcosm-universe-mock.html (frame() l.184-272, genShell() l.140-162 — the LOOK; nebula 213-223, stars 228-246, ignite 249-252, core 256-263, vignette 267-269)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (mapping table 184-198; Pitfall 4 no-radial-gradient 325-329; Open Q2 Crystalline-as-variant 421-424)
    - .planning/phases/02-visual-engine-simulator/02-CONTEXT.md (D-04 mock-parity-elevated; D-05 Crystalline ice-blue/faceted/high-symmetry)
  </read_first>
  <action>
    Create `src/styles/crystalline.ts` exporting `export const crystalline: StyleTemplate = StyleTemplateSchema.parse({ ... })` with `id: 'techno'` (NOT a new StyleId — avoids a contract change per Open Q2), an ice-blue/white `palette.ramp` (e.g. `['#04121a','#0e4d5e','#46e0d8','#e8fbff']` to match the genome), and `genes` mapping the facet gene to an angular/faceted primitive ref (e.g. `facet: 'star-polygon'`). Add it to `src/styles/index.ts`. Build `src/client/cosmos/paint.ts` (extracting/extending plan-01's draw) so a full `Scene` paints to mock parity using ONLY the mapping table: per-shell nebula clouds (additive glow Images tinted by shell hue, scaled to `shell.radius * Rmax`), small stars (tiny additive dots) + big stars (glow Image + bright core dot), the genesis core (large warm-white additive glow + bright dot), and a full-screen vignette texture drawn last. Resolve every `Element.hue`/`CoreNode.hue` (0..1) through `style.palette.ramp` — ALL look constants come from the StyleTemplate, none hard-coded in paint (PNT-02). For Crystalline, the painter selects the faceted primitive via `style.genes` — paint-only, no synthesis change. Frozen shells (`shells[1..]`) bake once via `bake.ts`; the frontier (`shells[0]`) stays live. Verify Phaser 4.2 signatures against `node_modules/phaser` types.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && grep -c "StyleTemplateSchema.parse" src/styles/crystalline.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'techno'" src/styles/crystalline.ts` returns 1 (Crystalline is a techno-id variant — no contract change, StyleIdEnum unchanged).
    - `grep -c "StyleTemplateSchema.parse" src/styles/crystalline.ts` returns 1 (schema-parsed data module, PNT-02).
    - `grep -c "BlendModes.ADD" src/client/cosmos/paint.ts` returns at least 1 (additive nebula/star/core glow == mock 'lighter').
    - paint reads style constants, not literals: `grep -cE "style\\.palette|\\.palette\\.ramp|style\\.genes" src/client/cosmos/paint.ts` returns at least 1 (PNT-02), and the painter never imports a genome or DayVector: `grep -cE "DayVector|genomes/" src/client/cosmos/paint.ts` returns 0 (ENG-02 seam).
    - `grep -c "addDynamicTexture\|bake" src/client/cosmos/paint.ts` returns at least 1 (frozen shells baked, frontier live — PNT-03).
    - `npm run type-check` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The canvas paints full mock parity (nebula, stars, frontier, core, vignette) entirely from StyleTemplate data, and Crystalline is a distinct ice-blue/faceted techno-id variant with no contract change — PNT-01/PNT-02.</done>
</task>

<task type="auto">
  <name>Task 2: Animate only the frontier in the frame loop and honor prefers-reduced-motion with a static frame</name>
  <files>src/client/cosmos/CosmosScene.ts, src/client/cosmos/reduced-motion.ts</files>
  <read_first>
    - src/client/cosmos/CosmosScene.ts (the Scene update()/frame loop to drive)
    - src/client/cosmos/paint.ts (the draw split: baked frozen vs live frontier)
    - docs/subcosm-universe-mock.html (the reduced-motion media query l.64 + matchMedia l.112; the reduce branch l.163,186,250,257 => static frame)
    - src/styles/techno.ts (motion.frontierOnly/speed — what drives animation)
    - .planning/phases/02-visual-engine-simulator/02-UI-SPEC.md (Motion & Reduced-Motion: "under reduced-motion the entire surface presents a static, non-strobe frame" — hard rule PNT-04)
    - .planning/phases/02-visual-engine-simulator/02-RESEARCH.md (Pitfall 6 reduced-motion lines 337-341; Pattern: frame loop animates only shells[0])
  </read_first>
  <action>
    In `CosmosScene.update()`, re-render ONLY the frontier (`shells[0]`) per frame — the pulsing ignite ring (animated alpha/lineWidth) + star twinkle, driven by `StyleTemplate.motion.speed`; all frozen shells stay as their baked Images (no per-frame redraw — PNT-03). Create `src/client/cosmos/reduced-motion.ts` that reads `window.matchMedia('(prefers-reduced-motion: reduce)')`; when reduced, render exactly ONE static frame (mock's reduce branch: ignite fully on, no pulse, no twinkle, no rotation) and STOP the animation loop — bake the frontier too so the entire surface is a still, non-strobe image. Wire CosmosScene to consult this on boot and on media-query change. The reduced-motion behavior is driven by data (`motion.frontierOnly`/`speed`) where applicable, not a hard-coded branch in paint.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && grep -c "prefers-reduced-motion" src/client/cosmos/reduced-motion.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prefers-reduced-motion" src/client/cosmos/reduced-motion.ts` returns at least 1 AND `grep -c "matchMedia" src/client/cosmos/reduced-motion.ts` returns at least 1 (PNT-04 honored).
    - The frame loop animates only the frontier: `grep -cE "shells\\[0\\]|frontier" src/client/cosmos/CosmosScene.ts` returns at least 1, and frozen-shell Images are not redrawn per frame (baked once — confirmed by bake usage from Task 1).
    - `npm run type-check` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Only the frontier animates each frame (frozen shells baked, PNT-03); under prefers-reduced-motion the whole surface is a single static, non-strobe frame and the loop stops (PNT-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify mock parity, the distinct Crystalline look, and the static reduced-motion frame</name>
  <files>(verification only — no files modified)</files>
  <action>Human visual verification of the paint output against the mock. No code changes.</action>
  <what-built>Full mock-parity Techno paint (genesis core, concentric shells, cyan↔magenta nebula, small+big stars, pulsing frontier ignite, vignette), a distinct ice-blue/faceted Crystalline StyleTemplate, and a static reduced-motion frame — all from StyleTemplate data, frozen shells baked + only the frontier animating.</what-built>
  <how-to-verify>
    1. Run the dev page via the `cosmos` script (plain vite) and open it next to `docs/subcosm-universe-mock.html` in a second window.
    2. Side-by-side, confirm the genesis core, concentric shells, cyan↔magenta nebula, frontier ignite, and vignette read as visually equivalent (mock-parity-elevated, D-04) — convincing, self-authored, not AI-slop. This is Success Criterion 1.
    3. Switch the genome to Crystalline (the selector lands in plan 05 — for now hard-set `crystalline` in main.ts to check) and confirm an ice-blue, faceted, high-symmetry look clearly distinct from Calm/Chaotic Techno (D-05).
    4. In OS settings (or DevTools "Emulate prefers-reduced-motion: reduce"), enable reduced motion and reload: confirm the ENTIRE canvas is a static, non-strobe frame — no pulsing, no twinkle (PNT-04 hard rule, Success Criterion 6 part).
  </how-to-verify>
  <resume-signal>Type "approved" once parity, the distinct Crystalline look, and the static reduced-motion frame all hold — or describe what's off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene → paint | Geometry-only Scene + StyleTemplate cross into the painter; paint must not reach for raw DayVector or a genome |
| OS motion preference → canvas | prefers-reduced-motion is an accessibility input the canvas must honor across the whole surface |

> Offline render slice — no network/auth/persistence. Threats are integrity/determinism + accessibility-correctness.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | paint hard-coding look constants instead of reading StyleTemplate | mitigate | Task 1 acceptance greps that paint reads `style.palette`/`style.genes` and imports no genome/DayVector (PNT-02 + ENG-02 seam) |
| T-02-08 | DoS (availability) | per-frame gradient/Graphics allocation tanking framerate | mitigate | Reused glow texture + baked frozen shells; only the frontier re-renders per frame (PNT-03); manual framerate check at the checkpoint |
| T-02-09 | Tampering (accessibility integrity) | reduced-motion not honored across the canvas (strobe risk) | mitigate | reduced-motion.ts stops the loop + renders one static frame; checkpoint step 4 verifies a fully static surface (PNT-04) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — Phaser pre-installed + CI-green from Phase 1 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run type-check && npm run lint && npm test && npm run build` all green.
- Human-verify checkpoint confirms: mock-parity side-by-side (SC-1), distinct Crystalline (D-05), static reduced-motion frame (PNT-04 / SC-6).
- paint reads only Scene + StyleTemplate (no DayVector/genome import).
</verification>

<success_criteria>
- Genesis core, shells, nebula, frontier ignite, and vignette match the mock side-by-side (SC-1, PNT-01).
- All Techno/Crystalline look constants come from StyleTemplate data, not paint literals (PNT-02).
- Crystalline reads ice-blue/faceted/high-symmetry, distinct at a glance (D-05).
- prefers-reduced-motion yields a static, non-strobe frame across the whole canvas (PNT-04).
- Frozen shells baked, only the frontier animates (PNT-03).
</success_criteria>

<output>
Create `.planning/phases/02-visual-engine-simulator/02-03-SUMMARY.md` when done.
</output>
