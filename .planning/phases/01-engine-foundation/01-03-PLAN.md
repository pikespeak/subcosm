---
phase: 01-engine-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/engine/genomes/chaotic.ts
  - src/engine/genomes/crystalline.ts
  - src/engine/genomes/index.ts
  - src/engine/presets.test.ts
autonomous: true
requirements: [TPL-03]
must_haves:
  truths:
    - "Three genome presets (Calm, Chaotic, Crystalline) exist as data files, each validated by GenomeSchema.parse, sharing the Techno style and differing only in genome data"
    - "synthesize(sameDayVectors, calm) vs (..., chaotic) vs (..., crystalline) produce visibly different Scenes — measurably differing element density / volatility / shell shape — with ZERO engine code change"
    - "The divergence is proven by a test asserting measurable Scene differences (element counts and/or JSON inequality) across the three presets on the identical DayVector[]"
  artifacts:
    - path: "src/engine/genomes/chaotic.ts"
      provides: "Chaotic preset (high volatility/density/conflict-reaction)"
      contains: "GenomeSchema.parse"
    - path: "src/engine/genomes/crystalline.ts"
      provides: "Crystalline preset (high symmetry + inheritance, low volatility)"
      contains: "GenomeSchema.parse"
    - path: "src/engine/genomes/index.ts"
      provides: "Barrel re-export of all three presets"
      contains: "calm"
    - path: "src/engine/presets.test.ts"
      provides: "TPL-03 divergence proof — same DayVector[], 3 presets, diverging Scenes"
      contains: "not.toBe"
  key_links:
    - from: "src/engine/genomes/chaotic.ts"
      to: "src/engine/contracts/index.ts"
      via: "GenomeSchema.parse validates the preset data at module load"
      pattern: "GenomeSchema\\.parse"
    - from: "src/engine/presets.test.ts"
      to: "src/engine/synthesis.ts"
      via: "runs the same fixtureDays through all three presets and asserts divergence"
      pattern: "synthesize\\("
---

<objective>
Prove the config-driven template engine claim (TPL-03): author the remaining two genome presets — Chaotic and Crystalline — as pure data, then prove that running the SAME `DayVector[]` through all three presets produces visibly different Scenes with ZERO engine code change.

Purpose: This is the payoff of the whole architecture bet — "provably different worlds from the same data + config". The presets are data files validated by `GenomeSchema.parse`; the divergence test is the evidence that a new genome is a data file, not an engine change. This makes the engine *do more* than Plan 02 (one preset proven) — it proves multiple distinct universes from one dataset.
Output: `genomes/chaotic.ts`, `genomes/crystalline.ts`, `genomes/index.ts`, and the TPL-03 divergence proof test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/01-engine-foundation/01-CONTEXT.md
@.planning/phases/01-engine-foundation/01-RESEARCH.md
@.planning/phases/01-engine-foundation/01-PATTERNS.md
@src/engine/genomes/calm.ts
@src/engine/synthesis.ts
@tests/fixtures.ts
</context>

<phase_goal>
**As a** community mod (and as the engine developer proving the framework), **I want to** switch between Calm, Chaotic, and Crystalline genome presets and see the SAME community data produce a visibly different universe, **so that** a community's look/behaviour is configuration (data), never an engine code change — the core template-engine promise.

> MVP note: This slice extends the Plan 02 pipeline. After it, the engine demonstrably synthesizes three distinct universes from one DayVector[] — strictly more capability than Plan 02's single-preset pipeline.
</phase_goal>

<artifacts_this_phase_produces>
This plan creates (Plan 03 of 3 in Phase 1 — final plan):
- `chaotic` genome preset — `src/engine/genomes/chaotic.ts` (`GenomeSchema.parse`, high volatility/density/conflict-reaction; D-01)
- `crystalline` genome preset — `src/engine/genomes/crystalline.ts` (`GenomeSchema.parse`, high symmetry + inheritance, low volatility, cool/faceted; D-01 — final palette/look deferred to Phase 2)
- `src/engine/genomes/index.ts` — barrel re-export of calm + chaotic + crystalline
- `src/engine/presets.test.ts` — the TPL-03 proof: same `fixtureDays` through all three presets → measurably diverging Scenes (element counts and/or JSON inequality) with no engine change
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Chaotic + Crystalline presets and the TPL-03 divergence proof</name>
  <files>src/engine/genomes/chaotic.ts, src/engine/genomes/crystalline.ts, src/engine/genomes/index.ts, src/engine/presets.test.ts</files>
  <behavior>
    - chaotic and crystalline parse cleanly via GenomeSchema.parse at module load (data, not code)
    - synthesize(fixtureDays, calm) vs (..., chaotic): differ in element density / volatility (e.g. frontier element count differs) — TPL-03
    - synthesize(fixtureDays, calm) vs (..., crystalline): differ (JSON.stringify inequality) — TPL-03
    - all three are still individually byte-identical deterministic (re-running each preset reproduces its Scene)
  </behavior>
  <read_first>
    - src/engine/genomes/calm.ts (the Calm preset authored in Plan 02 — copy its shape, flip the knobs to the opposite poles)
    - src/engine/contracts/Genome.ts + index.ts (the exact Genome field names the presets must set — density/volatility/spread/symmetry/inheritance knobs synthesis reads)
    - src/engine/synthesis.ts (which Genome knobs synthesis actually consumes — so the presets move the fields that visibly change the Scene)
    - tests/fixtures.ts (the shared fixtureDays the divergence test runs through all three presets)
    - .planning/phases/01-engine-foundation/01-CONTEXT.md D-01 (Calm/Chaotic = max-contrast poles: volatility/density/conflict-reaction; Crystalline = high symmetry + inheritance, cool/faceted; final palette Phase 2), D-02 (presets ARE the TPL-03 proof)
    - .planning/phases/01-engine-foundation/01-RESEARCH.md Pattern 2 (Genome-as-data lines 224-238), TPL-03 divergence test example (lines 406-417), Open Question 3 (Crystalline numeric knobs — Claude's discretion: low volatility, high inheritance, high symmetry/arms)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md genomes section (lines 204-206)
  </read_first>
  <action>
    Create `src/engine/genomes/chaotic.ts`: `export const chaotic: Genome = GenomeSchema.parse({ ... })` at the OPPOSITE pole from Calm — high `volatility`, low `inheritance`, density/spread/conflict-reaction knobs high; same `style: 'techno'`; `actionCap: 3`; a `dailyGoal`. `weights` typed-but-unused (D-05).
    Create `src/engine/genomes/crystalline.ts`: `export const crystalline: Genome = GenomeSchema.parse({ ... })` — high symmetry knobs (more arms), high `inheritance`, low `volatility`, cool/faceted character; same `style: 'techno'` (Phase 1); `actionCap: 3`; a `dailyGoal`. Numeric knobs are Claude's discretion, tuned so the divergence test below shows visible difference from Calm and Chaotic. Final palette/look is Phase 2.
    Create `src/engine/genomes/index.ts`: barrel re-exporting `calm`, `chaotic`, `crystalline`.
    Create `src/engine/presets.test.ts`: run the shared `fixtureDays` through `synthesize(...)` with each of the three presets. Assert measurable divergence: e.g. the frontier (or a dense-day) shell element count differs between calm and chaotic (`expect(elems(calm)).not.toBe(elems(chaotic))`), and `JSON.stringify(calmScene) !== JSON.stringify(crystallineScene)`. Also assert each preset is independently deterministic (re-synthesizing the same preset → byte-identical). Do NOT modify synthesis.ts — divergence must come purely from genome data (if a knob does not move the Scene, adjust the PRESET data, never add an engine branch).
    Verify `src/engine/synthesis.ts` and `src/engine/render.ts` are byte-unchanged by this plan (TPL-03 = zero engine code change).
  </action>
  <verify>
    <automated>npx vitest run src/engine/presets.test.ts --reporter=dot && npx vitest run src/engine --reporter=dot && npm run type-check && npm run lint && (git diff --quiet -- src/engine/synthesis.ts src/engine/render.ts 2>/dev/null && echo "ENGINE_UNCHANGED_OK" || echo "ENGINE_UNCHANGED_CHECK_MANUALLY")</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/genomes/chaotic.ts` and `crystalline.ts` each use `GenomeSchema.parse(...)` and parse without throwing at module load (data, not code); both set `style: 'techno'` and `actionCap: 3`.
    - `npx vitest run src/engine/presets.test.ts` exits 0; the test asserts `synthesize(fixtureDays, calm)` and `synthesize(fixtureDays, chaotic)` differ measurably (e.g. a shell's element count differs — `not.toBe`), and `JSON.stringify(calmScene) !== JSON.stringify(crystallineScene)`.
    - The divergence is produced with ZERO engine change: `synthesis.ts` and `render.ts` are byte-unchanged by this plan (`git diff --quiet -- src/engine/synthesis.ts src/engine/render.ts` is clean, or any diff is justified and re-reviewed). No `if (preset === ...)` branch exists in synthesis (`grep -c "preset ===" src/engine/synthesis.ts` returns 0).
    - Each preset remains independently deterministic (re-synthesizing the same preset yields a byte-identical Scene).
    - `npm run type-check` and `npm run lint` both exit 0; the full `npx vitest run src/engine` suite is green.
  </acceptance_criteria>
  <done>Three genome presets exist as validated data files; running the same DayVector[] through Calm/Chaotic/Crystalline yields measurably divergent Scenes with zero engine code change; the TPL-03 divergence proof passes; tsc + lint + full engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset data → GenomeSchema.parse | Each preset is validated at module load by `GenomeSchema.parse` — a malformed preset fails fast at import, never reaching synthesis. |
| genome data → synthesis | All preset-driven variation flows through Genome fields synthesis reads; no engine branch keys off the preset name (integrity of the "data not code" claim). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (template-engine claim violated) | divergence mechanism | mitigate | Divergence must come purely from Genome data; the test forbids engine changes (`git diff --quiet` on synthesis.ts/render.ts) and a `grep` confirms no `preset ===` branch in synthesis — if a knob does not move the Scene, the PRESET data is adjusted, never the engine. |
| T-03-02 | Tampering (malformed preset reaches synthesis) | preset data files | mitigate | `GenomeSchema.parse(...)` at module load rejects an invalid preset before it can be synthesized (fail-fast at import). |
| T-03-03 | Tampering (determinism regression) | presets.test.ts | mitigate | The test re-asserts per-preset byte-identical determinism, so adding presets cannot silently break the SYN-02 guarantee established in Plan 02. |
</threat_model>

<verification>
- `npx vitest run src/engine/presets.test.ts` green — same DayVector[], three presets, diverging Scenes (TPL-03).
- `npx vitest run src/engine` full engine suite green; `npm run type-check` + `npm run lint` green.
- `git diff --quiet -- src/engine/synthesis.ts src/engine/render.ts` — no engine code change (divergence is data-only).
- `grep "preset ===" src/engine/synthesis.ts` returns nothing.
</verification>

<success_criteria>
- ≥2 (here, three) selectable Genome presets, same Techno style, different params; switching presets visibly changes the universe from the same DayVector[] with zero engine code change (TPL-03).
- Calm vs Chaotic are max-contrast poles (density/volatility/conflict-reaction); Crystalline is high-symmetry + high-inheritance (D-01).
- Each preset is data validated by GenomeSchema.parse — a new genome is a data file, not an engine change.
</success_criteria>

<output>
Create `.planning/phases/01-engine-foundation/01-03-SUMMARY.md` when done.
</output>
