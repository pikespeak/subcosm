---
phase: 01-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tools/tsconfig.engine.json
  - tsconfig.json
  - eslint.config.js
  - vitest.config.ts
  - src/engine/contracts/DayVector.ts
  - src/engine/contracts/Scene.ts
  - src/engine/contracts/Genome.ts
  - src/engine/contracts/StyleTemplate.ts
  - src/engine/contracts/Personal.ts
  - src/engine/contracts/index.ts
  - src/engine/contracts/contracts.test.ts
autonomous: true
requirements: [ENG-01, ENG-03, TPL-02, TPL-04, GAME-01, GAME-05]
must_haves:
  truths:
    - "All four contract types (DayVector, Scene, Genome, StyleTemplate) are z.infer of a Zod schema — no hand-written interface exists for any of them"
    - "src/engine/ is a separate tsconfig project and a separate ESLint override that bans Math.random and @devvit/* / phaser imports"
    - "A planted Math.random() or `import 'phaser'` inside src/engine/ fails npm run lint"
    - "Genome schema includes dailyGoal (type + targetParam + threshold + direction) and actionCap (default 3)"
    - "DayVector includes an outcome field; Scene includes a goalAchieved field"
    - "A personal-layer schema (ActionBudget/PersonalState) exists, is exported as a distinct type, and contains no community-layer field; Scene contains no actionsUsed/userId field"
  artifacts:
    - path: "tools/tsconfig.engine.json"
      provides: "Isolated engine TS project (references nothing)"
      contains: "src/engine"
    - path: "eslint.config.js"
      provides: "src/engine/** override banning Math.random + @devvit/phaser imports"
      contains: "no-restricted-properties"
    - path: "src/engine/contracts/DayVector.ts"
      provides: "DayVectorSchema + outcome field; DayVector = z.infer"
      contains: "z.infer"
    - path: "src/engine/contracts/Scene.ts"
      provides: "SceneSchema/ShellSchema/ElementSchema/CoreNodeSchema + goalAchieved"
      contains: "z.infer"
    - path: "src/engine/contracts/Genome.ts"
      provides: "GenomeSchema (full §6.3 surface) + DailyGoalSchema + actionCap"
      contains: "z.infer"
    - path: "src/engine/contracts/StyleTemplate.ts"
      provides: "StyleTemplateSchema (typed now, Techno instance authored Phase 2)"
      contains: "z.infer"
    - path: "src/engine/contracts/Personal.ts"
      provides: "ActionBudget/PersonalState personal-layer schema, distinct from Scene"
      contains: "z.infer"
    - path: "src/engine/contracts/index.ts"
      provides: "Barrel re-export of all schemas + inferred types"
  key_links:
    - from: "tsconfig.json"
      to: "tools/tsconfig.engine.json"
      via: "references[] array entry so tsc --build picks up src/engine"
      pattern: "tsconfig.engine.json"
    - from: "src/engine/contracts/index.ts"
      to: "src/engine/contracts/DayVector.ts"
      via: "re-export of DayVectorSchema + DayVector type"
      pattern: "DayVector"
---

<objective>
Establish the engine's type backbone and isolation boundary: add zod + vitest, create the isolated `src/engine/` TypeScript project + the ESLint determinism boundary, and author the four Zod contract schemas (DayVector, Scene, Genome, StyleTemplate) plus the personal-layer schema — every type `z.infer` only, with the game-loop hook fields (`dailyGoal`, `outcome`, `goalAchieved`, `actionCap`) baked in.

Purpose: Everything downstream (synthesis, presets, paint, Devvit) imports these schemas. They are the single source of truth (CLAUDE.md §1). The personal-layer separation and goal fields are built now (build-for-flexibility) so the Phase 4 game loop and post-MVP monetization bolt on without reworking synthesis.
Output: zod+vitest installed; `tools/tsconfig.engine.json` + root reference; `src/engine/**` ESLint override (Math.random + Devvit/phaser banned); `vitest.config.ts`; five schema modules + barrel + a contracts test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/01-engine-foundation/01-CONTEXT.md
@.planning/phases/01-engine-foundation/01-RESEARCH.md
@.planning/phases/01-engine-foundation/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following symbols and files (Plan 01 of 3 in Phase 1):

**Config / tooling**
- `zod@4.4.3` (runtime dep) + `vitest@4.1.9` (dev dep) added to `package.json`; `"test": "vitest run"` script
- `tools/tsconfig.engine.json` (new isolated TS project; references nothing)
- `tsconfig.json` (modified: one new `references[]` entry → `./tools/tsconfig.engine.json`)
- `eslint.config.js` (modified: new `src/engine/**` override block banning `Math.random` + `@devvit/*` + `phaser` + cross-dir imports)
- `vitest.config.ts` (new; discovers `src/engine/**/*.test.ts`, does NOT load the Devvit Vite plugin)

**Contract schemas + inferred types (all `z.infer`, no hand-written interfaces)**
- `DayVectorSchema` / `DayVector` (incl. `outcome` field) — `src/engine/contracts/DayVector.ts`
- `SceneSchema` / `Scene`, `ShellSchema` / `Shell`, `ElementSchema` / `Element`, `CoreNodeSchema` / `CoreNode` (incl. `goalAchieved`) — `src/engine/contracts/Scene.ts`
- `GenomeSchema` / `Genome`, `DailyGoalSchema` / `DailyGoal`, `GoalTypeEnum`, `WeightsSchema` (typed-but-unused, D-05), `actionCap` field (default 3, D-04) — `src/engine/contracts/Genome.ts`
- `StyleTemplateSchema` / `StyleTemplate` (typed now, Techno instance authored Phase 2) — `src/engine/contracts/StyleTemplate.ts`
- `ActionBudgetSchema` / `ActionBudget` (or `PersonalState`) — personal layer — `src/engine/contracts/Personal.ts`
- barrel re-export — `src/engine/contracts/index.ts`

Plan 02 adds: `mulberry32`, `synthesize`, `render` stub, the `calm` preset. Plan 03 adds: the `chaotic` + `crystalline` presets + the TPL-03 divergence proof.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zod + vitest, create the isolated engine TS project, ESLint determinism boundary, and vitest config</name>
  <files>package.json, package-lock.json, tools/tsconfig.engine.json, tsconfig.json, eslint.config.js, vitest.config.ts</files>
  <read_first>
    - package.json (self — exact dep/script style to extend)
    - tools/tsconfig.shared.json (THE analog — copy this leaf-project structure exactly; PATTERNS.md lines 37-49)
    - tools/tsconfig.base.json (inherited strict flags: composite, noUncheckedIndexedAccess, exactOptionalPropertyTypes — engine inherits all)
    - tools/tsconfig.server.json (CONTRAST analog — shows a references[] block the engine must OMIT)
    - tsconfig.json (self — append one references[] entry, match existing literal style; PATTERNS.md lines 67-79)
    - eslint.config.js (the `shared` override block lines 20-31 is the shape analog; the trailing catch-all lines 45-67 is the ordering hazard — RESEARCH Pitfall 5)
    - vite.config.ts (defineConfig idiom analog for vitest.config.ts)
    - .planning/phases/01-engine-foundation/01-RESEARCH.md (ESLint engine-boundary override example lines 419-441; tsconfig wiring note line 201)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md (config analog sections + "do NOT ban Math globally")
  </read_first>
  <action>
    Run `npm install zod` (pin 4.4.3) and `npm install -D vitest` (pin 4.1.9); add `"test": "vitest run"` to package.json scripts beside the existing `"type-check": "tsc --build"`. Commit the updated package-lock.json.
    Create `tools/tsconfig.engine.json` by copying tools/tsconfig.shared.json and adapting: `rootDir` → `../src/engine`, `include` → `["../src/engine/**/*"]`, `outDir`/`tsBuildInfoFile` → `../dist/types/engine`, `lib` → `["ES2023"]` (drop WebWorker — synthesis is DOM-free). It must contain NO `references[]` block (engine is isolated — must not reference client/server/shared).
    Modify tsconfig.json: append `{ "path": "./tools/tsconfig.engine.json" }` to the `references[]` array.
    Modify eslint.config.js: add a new override block scoped to `files: ['src/engine/**/*.{ts,tsx}']` with `parserOptions.project: ['./tools/tsconfig.engine.json']` and `globals: globals.browser`, copying the `shared` block's `extends`/`languageOptions` shape. Add the ENG-03 rules: `no-restricted-imports` patterns for `@devvit/*`, `phaser`, `*/client/*`, `*/server/*`; and `no-restricted-properties` targeting object `Math` property `random` with an i18n-style message key pointing to engine/rng.ts. Use `no-restricted-properties` for Math.random ONLY — do NOT ban `Math` globally (synthesis needs Math.imul/max/floor/PI/cos/sin). Place the block AFTER the trailing catch-all so its rules win for src/engine/** (RESEARCH Pitfall 5).
    Create `vitest.config.ts` using the `defineConfig` idiom imported from `vitest/config`; configure it to discover `src/engine/**/*.test.ts`. Do NOT pull in the Devvit Vite plugin — the engine test run must not load Devvit.
    To prove the boundary works: create a scratch file `src/engine/_scratch_planted.ts` containing a line that calls `Math.random()` and a line that imports phaser, run `npm run lint`, confirm it errors on BOTH, then DELETE the scratch file.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -qiE "no-restricted-(properties|imports)" && echo PLANTED_FIXTURE_CAUGHT; rm -f src/engine/_scratch_planted.ts; node -e "const p=require('./package.json'); if(!p.dependencies.zod) throw new Error('zod missing'); if(!p.devDependencies.vitest) throw new Error('vitest missing'); if(!p.scripts.test) throw new Error('test script missing');" && npx vitest run --reporter=dot 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('zod')"` resolves (zod installed) and `package.json` dependencies contains `zod` and devDependencies contains `vitest`, scripts contains `"test": "vitest run"`.
    - `tools/tsconfig.engine.json` exists, has `"rootDir": "../src/engine"`, and contains NO `references` key (`grep -c '"references"' tools/tsconfig.engine.json` returns 0).
    - `tsconfig.json` `references[]` includes an entry whose path contains `tsconfig.engine.json`.
    - With a scratch file in src/engine/ that calls `Math.random()` and imports phaser, `npm run lint` exits non-zero and reports a `no-restricted-properties` AND a `no-restricted-imports` violation; after deleting the scratch file `npm run lint` is green for the (currently empty) engine tree.
    - `npx vitest run` exits 0 (no tests yet is acceptable, or only later-added contract tests run).
  </acceptance_criteria>
  <done>zod+vitest installed; isolated engine TS project wired into the root build; ESLint bans Math.random + Devvit/phaser inside src/engine/ (proven by a planted-then-deleted fixture); vitest discovers engine tests without loading Devvit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the four contract Zod schemas + personal-layer schema + barrel, all z.infer, with goal/outcome/actionCap fields and a schema-inspection test</name>
  <files>src/engine/contracts/DayVector.ts, src/engine/contracts/Scene.ts, src/engine/contracts/Genome.ts, src/engine/contracts/StyleTemplate.ts, src/engine/contracts/Personal.ts, src/engine/contracts/index.ts, src/engine/contracts/contracts.test.ts</files>
  <read_first>
    - docs/subcosm-requirements.md §6.1 DayVector (lines 113-133), §6.2 Scene/Shell/Element (lines 135-149), §6.3 Genome incl. weights matrix (lines 151-167), §6.4 StyleTemplate (lines 169-178) — the exact field SHAPES to translate (NOT the output form)
    - .planning/phases/01-engine-foundation/01-RESEARCH.md Code Examples: DayVectorSchema (lines 312-339), GoalTypeEnum/DailyGoalSchema (lines 341-359), WeightsSchema (lines 361-374) — the authored Zod translations to follow
    - .planning/phases/01-engine-foundation/01-CONTEXT.md decisions D-03 (dailyGoal enum mix), D-04 (actionCap default 3, personal layer), D-05 (full §6.3 surface incl. weights, typed-but-unused)
    - .planning/phases/01-engine-foundation/01-PATTERNS.md contracts section (lines 194-206) — z.infer-only mandate; Personal.ts has no analog (lines 240-245)
    - CLAUDE.md §1 / §9 — z.infer only, no duplicate interfaces, no `as` casts, i18n error keys
    - src/shared/api.ts — the ANTI-pattern (hand-written type aliases); the engine must do the OPPOSITE
  </read_first>
  <action>
    Create `src/engine/contracts/DayVector.ts`: `DayVectorSchema` = z.object covering §6.1 (day, date, posts, comments, contributors, scoreSum, topThreads:number[], conflict 0..1, momentum -1..1, diversity 0..1, dominantTheme, steering:{branch,symmetry,hue}, seed:int) PLUS an `outcome` field (GAME-01 scoring hook; loose now per RESEARCH A3 — e.g. optional, shape firmed Phase 4). Export `type DayVector = z.infer<typeof DayVectorSchema>`.
    Create `src/engine/contracts/Scene.ts`: `CoreNodeSchema`, `ElementSchema` (kind enum star|cluster|filament, angle, r, size, energy, hue as a 0..1 HINT not a color, conflict, big), `ShellSchema` (day, radius, meta:{date,era,theme,posts,comments,contributors,conflict}, elements:Element[]), `SceneSchema` (core, shells[]) PLUS a `goalAchieved` field (GAME-01; loose now — e.g. boolean nullable). Export each `z.infer` type.
    Create `src/engine/contracts/Genome.ts`: `GoalTypeEnum` (D-03 mix: reachSymmetry, conflictBelow, igniteRareGene, starThreshold, densityThreshold, contributorCount), `DailyGoalSchema` ({type, targetParam, threshold, direction:above|below}), `WeightsSchema` (§6.3 Signal→Param matrix — typed-but-unused per D-05; re-verify the Zod v4 z.record/z.partialRecord helper via `ctx7 docs /colinhacks/zod` at impl time), and `GenomeSchema` covering the full §6.3 surface (version, style, palette, weights, ranges, baseVar, volatility, inheritance, steerGain, rareTable, allowedGenes, dayBoundary) PLUS `dailyGoal` and `actionCap` (default 3, D-04). Export `z.infer` types.
    Create `src/engine/contracts/StyleTemplate.ts`: `StyleTemplateSchema` covering §6.4 (id, substrate, palette, line, fill, texture, genes, postFX, motion, type) — typed now; the Techno instance is authored in Phase 2. Export `z.infer` type.
    Create `src/engine/contracts/Personal.ts`: `ActionBudgetSchema` (or `PersonalState`) — personal layer (e.g. actionsUsed, cap, userId/dayKey). It must contain NO community-layer field and must be a structurally distinct exported type. Do NOT embed it in Scene.
    Create `src/engine/contracts/index.ts`: barrel re-exporting every schema + inferred type.
    Any schema error messages must use i18n keys, not hardcoded language (CLAUDE.md §7).
    Create `src/engine/contracts/contracts.test.ts`: (a) parse a valid DayVector/Genome/StyleTemplate fixture; (b) `safeParse` rejects an invalid one (e.g. conflict > 1); (c) assert presence of `dailyGoal` on Genome, `outcome` on DayVector, `goalAchieved` on Scene, `actionCap` default 3 on a parsed Genome; (d) assert SceneSchema has NO `actionsUsed`/`userId` key and that the personal-layer type is distinct (GAME-05 separation).
  </action>
  <verify>
    <automated>npx vitest run src/engine/contracts -t "contracts" --reporter=dot && npm run type-check && grep -rEL "z.infer" src/engine/contracts/DayVector.ts src/engine/contracts/Scene.ts src/engine/contracts/Genome.ts src/engine/contracts/StyleTemplate.ts src/engine/contracts/Personal.ts | grep -q . && echo "MISSING_ZINFER" || echo "ALL_ZINFER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engine/contracts` exits 0; the contracts test asserts a valid fixture parses, an invalid one is rejected by safeParse, and the goal/outcome/goalAchieved/actionCap fields are present (actionCap defaults to 3).
    - `npm run type-check` (`tsc --build`) exits 0 with the new engine project.
    - `grep -rE "interface (DayVector|Scene|Genome|StyleTemplate|Shell|Element|CoreNode|ActionBudget|PersonalState)" src/engine` returns NOTHING (no hand-written interfaces — ENG-01).
    - Every contract module exports its type via `z.infer` (grep confirms `z.infer` present in each of the five contract files).
    - The contracts test asserts SceneSchema has no `actionsUsed`/`userId` field and that the personal-layer type is a distinct export (GAME-05).
  </acceptance_criteria>
  <done>The four contracts + personal layer exist as Zod schemas with z.infer types only; goal/outcome/goalAchieved/actionCap fields exist and are schema-inspectable; the personal layer is structurally separate from Scene; contracts test + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (future) sim/Redis → engine | Untrusted DayVector data will cross here in Phase 2/3 via `DayVectorSchema.parse()`. In Phase 1 there is NO live boundary — only test fixtures cross it. The schemas authored here ARE the future guard. |
| build/lint → engine source | The ESLint engine override is the integrity boundary that keeps non-deterministic entropy and Devvit coupling out of `src/engine/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (integrity) | src/engine/ source | mitigate | ESLint `src/engine/**` override bans `Math.random`, `@devvit/*`, `phaser`, cross-dir imports — set up BEFORE any synthesis code (Task 1); proven by a planted-then-deleted fixture. |
| T-01-02 | Tampering (data integrity, latent) | DayVector/Genome inputs | mitigate (deferred enforcement) | Schemas authored now with bounded fields (conflict 0..1, momentum -1..1); `.parse()` enforcement lands at the Phase 2 sim→engine boundary (QA-03). Phase 1 trusts z.infer types inside the engine — no live untrusted input exists yet. |
| T-01-SC | Tampering (supply chain) | npm installs (zod, vitest) | mitigate | RESEARCH Package Legitimacy Audit: zod (201.9M/wk, official colinhacks/zod) Approved; vitest (71.3M/wk, official vitest-dev/vitest) SUS-too-new → resolved Approved (no postinstall scripts). Pin versions; commit lockfile. No `[ASSUMED]`/`[SLOP]` package → no blocking human checkpoint required. |
| T-01-03 | Spoofing (RNG misuse) | future rng.ts | accept | `mulberry32` is non-cryptographic by design (determinism > unpredictability). Documented in Plan 02. Never seeds secrets/tokens. Low risk: no security context in a pure engine. |
</threat_model>

<verification>
- `npm run lint` green on the engine tree; planted Math.random()/phaser-import fixture fails lint (then deleted).
- `npm run type-check` (`tsc --build`) green with the new engine project reference.
- `npx vitest run src/engine/contracts` green.
- `grep -rE "interface (DayVector|Scene|Genome|StyleTemplate)" src/engine` returns nothing.
</verification>

<success_criteria>
- All four contracts + the personal layer are Zod schemas; every type is `z.infer` (ENG-01).
- src/engine/ is an isolated TS project + ESLint override that bans Math.random + Devvit/phaser (ENG-03).
- Genome carries per-community knobs as data incl. the full §6.3 surface (TPL-02); style is genome-driven via `Genome.style` (TPL-04).
- `dailyGoal` (Genome), `outcome` (DayVector), `goalAchieved` (Scene) fields exist (GAME-01); `actionCap` + a distinct personal layer exist (GAME-05).
</success_criteria>

<output>
Create `.planning/phases/01-engine-foundation/01-01-SUMMARY.md` when done.
</output>
