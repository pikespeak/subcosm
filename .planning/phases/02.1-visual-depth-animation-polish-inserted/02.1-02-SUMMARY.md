---
phase: 02.1-visual-depth-animation-polish-inserted
plan: 02
subsystem: ui
tags: [phaser, animation, ignite, paint, vitest, no-strobe, reduced-motion]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: per-shell `weight` field (Plan 01 VIS-DEPTH) folded into the baked per-glow alpha
  - phase: 02
    provides: PhaserPainter / CosmosScene / paint.ts / Scene seam, frontier ignite loop
provides:
  - Pure unit-tested `igniteParams(conflict, energy, time, speed)` with a mathematical no-strobe bound (pulse ∈ [0.60, 0.96])
  - Data-driven frontier ignite — conflict→amplitude/edge-hardness, energy→tempo — wired into CosmosScene.update() below the reduced-motion gate
  - Baked per-day metric signature on each frozen shell (D-05), zero per-frame cost
affects: [Phase 3 Devvit wiring (client render parity), Phase 2.2 universe density, VIS-ANIM]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Extract per-frame animation math into a pure, side-effect-free fn (ignite.ts) so the no-strobe bound is unit-testable in isolation"
    - "Bake per-day character into frozen shells by modulating each draw call's OWN args before bakeShell — never via dt.draw alpha/tint (silently ignored on game objects)"
    - "Cache the frontier-energy scalar in layout()/repaintFrontier(); never reduce() per frame in the hot path"

key-files:
  created:
    - src/client/cosmos/ignite.ts
    - src/client/cosmos/ignite.test.ts
  modified:
    - src/client/cosmos/CosmosScene.ts
    - src/client/cosmos/paint.ts

key-decisions:
  - "ignite.ts is a pure fn of (conflict, energy, time, speed) — no Phaser/DOM/rng — so the no-strobe bound is a mathematical, unit-tested guarantee not a visual hope"
  - "No-strobe bound locked at pulse ∈ [SHIMMER_BASE − AMPLITUDE_MAX, SHIMMER_BASE + AMPLITUDE_MAX] = [0.60, 0.96], min far from 0 (D-04)"
  - "All new pulse math sits below the unchanged `if (!this.animate || !this.paintResult) return;` gate — reduced-motion stays a single static frame (PNT-04); static/initial/post-nudge frames use igniteParams rest values so animated baseline and static frame agree"
  - "Frozen-shell character is baked into per-glow args (conflict/energy/weight → tint/alpha/size/count) before bakeShell — zero per-frame cost, 60fps rule intact (D-05, PNT-03)"
  - "paint reads only Scene metrics (shell.meta.conflict, shell.weight, mean element energy) — never a raw DayVector (ENG-02); no rng in paint"
  - "Tuning constants (SHIMMER_BASE, AMPLITUDE_MAX, twinkle base/cap, energy→tempo coefficients, signature redshift/cluster/brightness strengths) are [ASSUMED] — accepted as-is at the Task 4 visual checkpoint"

patterns-established:
  - "Pure-fn extraction for testable animation bounds: per-frame visual math → its own file with exported bound constants the test asserts"
  - "Baked-signature-not-motion: frozen shells gain per-day character through bake-time draw args, never per-frame work"

requirements-completed: [VIS-ANIM]

# Metrics
duration: ~9min
completed: 2026-06-20
status: complete
---

# Phase 02.1 Plan 02: Data-Driven Frontier Ignite + Baked Frozen Character (VIS-ANIM) Summary

**Data-driven frontier ignite via a pure unit-tested `igniteParams` (conflict→amplitude/hardness, energy→tempo, mathematically bounded no-strobe pulse [0.60, 0.96]) wired into CosmosScene below the reduced-motion gate, plus a baked per-day metric signature on each frozen shell — paint-side only, no engine contract or snapshot change.**

## Performance

- **Duration:** ~9 min (continuation close-out; Tasks 1–3 executed in the prior run)
- **Completed:** 2026-06-20
- **Tasks:** 4 (3 code + 1 blocking human-verify checkpoint, APPROVED)
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- New pure `src/client/cosmos/ignite.ts` — `igniteParams(conflict, energy, time, speed) → { pulse, twinkle }` with exported bound constants; no Phaser/DOM/rng dependency.
- Unit-tested no-strobe guarantee: `pulse` (and `twinkle`) stay within `[floor, cap]` and never reach 0 across the full conflict/energy/time/speed domain; conflict→amplitude and energy→tempo differentiation proven; calm vs high-conflict envelopes measurably differ (6 tests).
- `CosmosScene.update()` rewired to call `igniteParams` reading `shells[0].meta.conflict` + a cached `frontierEnergy` scalar; legacy strobing `0.55 + 0.45*sin` / `0.72 + 0.28*sin` formulas removed. All new math below the unchanged reduced-motion gate; static/initial/post-nudge frames use igniteParams rest values.
- `paint.ts` `drawShell` injects a baked per-day signature (conflict/energy/weight → per-glow tint/alpha/size/count) onto each `addGlow`/`addFacetStar` call's own args before `bakeShell` — zero per-frame cost; folds the Plan-01 `weight` age-fade so D-02 and D-05 compose.
- Synthesis determinism snapshot untouched (paint-only plan): full suite 55/55 green, no re-baseline.

## Task Commits

1. **Task 1: Extract pure igniteParams + no-strobe bound test (RED→GREEN)** - `19bca4f` (test)
2. **Task 2: Wire igniteParams into CosmosScene.update() below the motion gate** - `445116b` (feat)
3. **Task 3: Bake per-day metric signature into each frozen shell (D-05)** - `8f19123` (feat)
4. **Task 4: Visual checkpoint (differentiated ignite + frozen character + reduced-motion)** - no code change; blocking human-verify, APPROVED by the developer.

## Files Created/Modified
- `src/client/cosmos/ignite.ts` - NEW. Pure `igniteParams` + exported bound constants (`SHIMMER_BASE`, `AMPLITUDE_MAX`, twinkle base/cap); the analytic no-strobe guarantee.
- `src/client/cosmos/ignite.test.ts` - NEW. Sweeps conflict∈[0,1], energy∈[0,1], time, speed; asserts pulse/twinkle ∈ [floor, cap] & >0, conflict→amplitude, energy→tempo, calm vs conflict differentiation.
- `src/client/cosmos/CosmosScene.ts` - update() calls igniteParams below the unchanged reduced-motion gate; cached `frontierEnergy` set in layout()/repaintFrontier() (no per-frame reduce); legacy sin formulas removed.
- `src/client/cosmos/paint.ts` - drawShell baked per-day signature from Scene metrics (no rng), applied to each glow/star's own args before bakeShell.

## Checkpoint Resolution

**Task 4 (blocking human-verify) — APPROVED by the developer.** Confirmed via side-by-side visual check vs `docs/subcosm-universe-mock.html` on the 30-day simulated universe:
- The frontier ignite is data-driven and visibly differentiated — conflict→amplitude/edge-hardness, energy→tempo, gentle shimmer — across a high-conflict day, a calm day, and an AMA/energy-spike day (D-03, ROADMAP success criterion #2).
- The ignite reads as a gentle shimmer and never strobes; the bound `pulse ∈ [0.60, 0.96]` is proven by `ignite.test.ts` (D-04).
- Frozen shells carry distinct baked per-day character — a high-conflict frozen day looks more turbulent/redshifted than a calm one, with no per-frame motion (D-05, 60fps rule intact).
- `prefers-reduced-motion` yields a single static, non-strobe frame (PNT-04, success criterion #5).

The animation work is accepted as-is. The [ASSUMED] tuning constants (SHIMMER_BASE, AMPLITUDE_MAX, twinkle base/cap, energy→tempo coefficients, signature redshift/cluster/brightness strengths) were accepted without further tuning.

## Decisions Made
See key-decisions in frontmatter. In short: pure-fn extraction for a mathematical no-strobe bound; all new motion below the reduced-motion gate; frozen character baked into draw args (never dt.draw, never per-frame); paint reads only Scene metrics (ENG-02), no rng; tuning constants [ASSUMED] and accepted at the checkpoint.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. Automated gate re-confirmed green at close-out: `npx vitest run src/client/cosmos/ignite.test.ts` (6/6), `npm test` (55/55, 8 files), `npx tsc --noEmit` clean.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- **VIS-ANIM complete** — Phase 2.1 (Visual Depth & Animation Polish) is now fully done (VIS-DEPTH from Plan 01 + VIS-ANIM here). Ready to advance to Phase 3 (Devvit Scaffold + Data Layer).
- **Carried-forward deferral (Phase 2.2 — universe density/richness):** simulator sparsity vs the mock (`src/sim/beats.ts` beat magnitudes) remains deferred to a dedicated Phase 2.2 on universe richness/density. This is NOT a VIS-ANIM or VIS-DEPTH defect — star elements are byte-identical; it is an orthogonal simulator-output richness gap to raise universe density/variety toward the mock. Carry forward.

## Self-Check: PASSED

- Files: `src/client/cosmos/ignite.ts`, `src/client/cosmos/ignite.test.ts`, `02.1-02-SUMMARY.md` — all present.
- Commits: `19bca4f`, `445116b`, `8f19123` (tasks), `832d34e` (summary), `6430892` (tracking) — all present.
- REQUIREMENTS.md: VIS-ANIM = `[x]`.

---
*Phase: 02.1-visual-depth-animation-polish-inserted*
*Completed: 2026-06-20*
