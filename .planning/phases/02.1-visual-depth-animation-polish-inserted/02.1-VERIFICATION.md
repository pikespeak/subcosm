---
phase: 02.1-visual-depth-animation-polish-inserted
verified: 2026-06-20T11:09:00Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
---

# Phase 02.1: Visual Depth & Animation Polish Verification Report

**Phase Goal:** Earlier/frozen day-shells read distinctly all the way toward the core (no central blob); the frontier ignite animation is data-driven so different days/communities animate differently; AND every ring reads as visibly populated — all without breaking determinism, the 60fps "only the frontier animates" rule, or the synthesis/paint contract separation. Judged vs `docs/subcosm-universe-mock.html`.
**Verified:** 2026-06-20T11:09:00Z
**Status:** passed
**Re-verification:** No — initial verification (post-REVIEW shipped state)

## Goal Achievement

This phase ran a code-review cycle (02.1-REVIEW.md: 1 BLOCKER CR-01, warnings WR-01/02/04) plus a scope addition (02.1-03, VIS-DENSITY). Verification targets the SHIPPED code, not the plans. Every flagged issue was confirmed fixed in the source.

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | VIS-DEPTH: every ring distinguishable to the core, no central blob, for ALL N incl. N≈30 (CR-01 fix) | ✓ VERIFIED | `synthesis.ts:117 RADIUS_FLOOR=0.12`; `shellRadius` re-maps falloff into `[RADIUS_FLOOR,1]` (`:169`) and floors the min-gap pull so it can never undershoot (`:173`); `minGapFor` uses `MIN_GAP_FRACTION=0.6` so `(n-1)*gap < 1-RADIUS_FLOOR` (`:126,138`). Test `depth geometry holds at N=%i` exercises N=1,2,3,30,50,80 asserting r>0, r>core+margin, strictly decreasing, gap≥minGapFor — **6/6 pass**. |
| 2 | VIS-DEPTH: older shells fade with age, standout days keep accents, none below legibility floor | ✓ VERIFIED | `shellWeight` (`:186`) = `ageFade*0.7 + standout*0.4` clamped `≥ LEGIBILITY_FLOOR=0.35` (`:105,195`); test `every shell carries a weight ≥ LEGIBILITY_FLOOR` and `high-conflict day keeps higher weight than calm later day` both green. |
| 3 | VIS-ANIM: ignite pulse visibly differs by day metrics (conflict→amplitude/hardness, energy→tempo), not a StyleTemplate constant | ✓ VERIFIED | Pure `igniteParams(conflict,energy,time,speed)` in `ignite.ts:79`; conflict→amp (`:93`)+edge-hardness via `shapeEdge` (`:56`), energy→tempo (`:90`). Tests assert monotone conflict→amplitude and energy→tempo (crossings) differentiation — green. CosmosScene reads `shells[0].meta.conflict` + cached `frontierEnergy`, not a StyleTemplate constant (`CosmosScene.ts:194-200`). |
| 4 | VIS-ANIM: ignite is a gentle shimmer, mathematically bounded away from 0, cannot strobe | ✓ VERIFIED | `pulse ∈ [SHIMMER_BASE−AMPLITUDE_MAX, SHIMMER_BASE+AMPLITUDE_MAX] = [0.60, 0.96]` (`ignite.ts:28-30`). Test sweeps conflict/energy/speed × 600 time samples asserting `pulse>0`, within band, floor>0.5 — green. |
| 5 | VIS-ANIM: only the frontier re-renders per frame; frozen shells baked; each frozen shell carries a baked per-day signature | ✓ VERIFIED | `paintScene` bakes `shells[1..]` once via `bakeShell` (`paint.ts:277-286`); only `shells[0]` redrawn per frame (`update()` → `renderFrontier`, `CosmosScene.ts:181-202,211`). Baked signature folds `shell.meta.conflict`/`weight`/meanEnergy into per-glow args before bake (`paint.ts:157-205`), zero per-frame cost. |
| 6 | VIS-ANIM: prefers-reduced-motion → single static non-strobe frame; new motion below the animate gate; WR-02 fix | ✓ VERIFIED | All shimmer math below `if (!this.animate || !this.paintResult) return;` (`CosmosScene.ts:189`). WR-02 fixed: `this.animate = this.style.motion.speed > 0 && !prefersReducedMotion()` (`:86,109`) — NO longer derived from `motion.frontierOnly`. Static frame uses `IGNITE_REST` (`:145,172`). |
| 7 | VIS-DENSITY: starCount floors so the quietest sim day is populated; busy>quiet contrast preserved | ✓ VERIFIED | `STAR_FLOOR=18`, `STAR_BASELINE=10` (`synthesis.ts:67,78`); `starCount = max(STAR_FLOOR, min(112, round(STAR_BASELINE+posts*density)))` (`:85`). Tests: every non-genesis shell ≥ STAR_FLOOR across real sim arc; drama day-12 ≥ 1.4× quiet day-30; cap 112 intact — green. |
| 8 | Determinism: same seed + genomeVersion → byte-identical Scene; elements byte-reproducible | ✓ VERIFIED | Geometry/weight/density helpers consume ZERO `rng()` (grep confirms `rng()` only in the seeded element loop, `synthesis.ts:252-263`). Tests: byte-identical Scene twice (`JSON.stringify` equality); elements deep-equal + byte-equal re-baselined snapshot — green. |
| 9 | Contract separation: synthesis stays Phaser-free; paint reads only Scene, never a raw DayVector | ✓ VERIFIED | `synthesis.ts` imports only `./rng` + contracts (`:23-31`), no Phaser/Devvit. `paint.ts`/`CosmosScene.ts`/`ignite.ts` read `shell.weight`/`shell.meta.conflict`/Element.energy — Scene values only; no raw DayVector import. |
| 10 | Engine purity boundary: no Math.random / no Devvit / no new rng() in src/engine/ | ✓ VERIFIED | grep: no `Math.random` or `@devvit` usage in `src/engine/` (only `rng.ts:5` comment documenting the ban). Seeded `mulberry32` only. |
| 11 | Automated gates green: npm test (~65), tsc --noEmit, npm run build | ✓ VERIFIED | `npm test` → **65 passed (8 files)**; `npx tsc --noEmit` → **exit 0**; `npm run build` → **complete** (only pre-existing unrelated Vite config warnings). |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/engine/synthesis.ts` | shellRadius (RADIUS_FLOOR + min-gap), shellWeight, starCount floor — all pure, no rng | ✓ VERIFIED | All three helpers present + exported constants; `Math.pow(0.85,idx)` removed; re-map into `[RADIUS_FLOOR,1]`; wired in `synthesize` map at `:284,291`. |
| `src/engine/synthesis.test.ts` | N=1,2,3,30,50,80 geometry + weight-floor + density + determinism asserts | ✓ VERIFIED | `test.each([1,2,3,30,50,80])` CR-01 regression; VIS-DENSITY describe block; re-baselined elements snapshot guard. |
| `src/engine/contracts/Scene.ts` | Shell.weight bounded Zod scalar, z.infer, no hand-written interface | ✓ VERIFIED | `weight: z.number().min(0).max(1)` on ShellSchema (`:52`); `type Shell = z.infer<...>` (`:56`). |
| `src/client/cosmos/ignite.ts` | pure igniteParams with exported no-strobe bound constants | ✓ VERIFIED | Pure fn, no Phaser/DOM/rng; SHIMMER_BASE/AMPLITUDE_MAX/twinkle exports; IGNITE_REST. |
| `src/client/cosmos/ignite.test.ts` | unit-tested bound + differentiation | ✓ VERIFIED | toBeGreaterThan(0) bound sweep + conflict→amp + energy→tempo tests. |
| `src/client/cosmos/CosmosScene.ts` | igniteParams below animate gate, cached frontierEnergy, WR-02 animate fix | ✓ VERIFIED | gate at `:189`; `frontierEnergy` cached in layout/repaintFrontier (no per-frame reduce); animate = speed>0 && !reduced. |
| `src/client/cosmos/paint.ts` | weight→alpha/size + baked per-day signature, no rng | ✓ VERIFIED | `drawShell` folds weight (`:142,144,189,202,205`) + baked conflict/energy signature (`:157-167`); frozen baked once. |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| synthesis.ts | Scene.ts | Shell literal emits `radius`+`weight` (fixed-key) | ✓ WIRED (`synthesis.ts:293-307`) |
| paint.ts | Scene.ts | drawShell reads `shell.weight` | ✓ WIRED (`paint.ts:142`) |
| CosmosScene.ts | ignite.ts | update() calls `igniteParams(...)` | ✓ WIRED (`CosmosScene.ts:195`) |
| CosmosScene.ts | Scene.ts | reads `shells[0].meta.conflict` + cached energy, no raw DayVector | ✓ WIRED (`CosmosScene.ts:194`) |
| paint.ts | Scene.ts | baked signature reads `shell.meta.conflict` | ✓ WIRED (`paint.ts:157`) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| CR-01 at-scale geometry (N=1..80) | `vitest -t "depth geometry holds"` | 6/6 pass | ✓ PASS |
| Full suite | `npm test` | 65 passed / 8 files | ✓ PASS |
| Type check | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Build | `npm run build` | complete | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
| ----------- | ----------- | ------ | -------- |
| VIS-DEPTH | 02.1-01 | ✓ SATISFIED | shellRadius/shellWeight, RADIUS_FLOOR fix, weight contract field; REQUIREMENTS.md `[x]`. |
| VIS-ANIM | 02.1-02 | ✓ SATISFIED | igniteParams + bound + CosmosScene wiring + baked signature; REQUIREMENTS.md `[x]`. |
| VIS-DENSITY | 02.1-03 (scope-add, no formal PLAN) | ✓ SATISFIED | STAR_FLOOR/STAR_BASELINE, density tests, re-baseline; REQUIREMENTS.md `[x]`. |

All three declared requirement IDs accounted for. No orphaned requirements.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
| ---- | ------- | -------- | ------ |
| (none) | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER in phase files; no Math.random/Devvit in src/engine/; no unwired/stub artifacts | — | None |

### Code-Review Issue Closure (02.1-REVIEW.md)

| Issue | Severity | Shipped State |
| ----- | -------- | ------------- |
| CR-01: negative radii at N≈30 | BLOCKER | ✓ FIXED — RADIUS_FLOOR re-map + band-sized minGapFor; N=30/50/80 regression tests pass. |
| WR-01: stale reduced-motion docstrings | warning | ✓ FIXED — docstrings reference IGNITE_REST/SHIMMER_BASE (`CosmosScene.ts:207-209`, `paint.ts:342-343`). |
| WR-02: animate derived from frontierOnly | warning | ✓ FIXED — animate gates on `speed>0 && !reducedMotion` (`CosmosScene.ts:86,109`). |
| WR-04: D-01 test only at N=3 | warning | ✓ FIXED — parametrized N=1,2,3,30,50,80 + per-N minGapFor. |

### Human Verification Required

None. The visual/"convincing vs mock" criteria (depth reads distinctly with no central blob, ignite differentiated and never strobes, every ring populated) were judged LIVE on the dev page and APPROVED by the developer at both blocking human-verify checkpoints (02.1-01 Task 3, 02.1-02 Task 4) — treated as developer-confirmed per the verification brief. All code-level must_haves and automated gates verified above.

### Gaps Summary

No gaps. The phase goal is achieved in the shipped codebase: VIS-DEPTH (no central blob to N≈30, CR-01 closed), VIS-ANIM (data-driven, mathematically no-strobe ignite + baked frozen character, WR-02 closed), and VIS-DENSITY (every ring populated, contrast preserved) all land end-to-end with determinism intact, the 60fps frontier-only rule preserved, synthesis/paint contract separation held, and all automated gates green (65 tests, tsc clean, build complete).

---

_Verified: 2026-06-20T11:09:00Z_
_Verifier: Claude (gsd-verifier)_
