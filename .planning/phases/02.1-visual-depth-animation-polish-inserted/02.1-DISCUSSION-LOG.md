# Phase 2.1: Visual Depth & Animation Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 2.1-Visual Depth & Animation Polish (INSERTED)
**Areas discussed:** Phase definition/insertion, Determinism handling, Depth geometry, Old-shell weight, Metric→motion mapping, Frozen-shell character

---

## Phase definition & insertion

Phase 2.1 did not exist in the roadmap. User chose to **define & insert** it (vs. discussing Phase 3 or just viewing roadmap state). Scoped from the VIS-DEPTH/VIS-ANIM items deferred in Plan 02-05. Phase definition (goal + 5 success criteria + VIS-DEPTH/VIS-ANIM as new requirements) presented and approved verbatim ("Ja, so eintragen"). Written to `.planning/ROADMAP.md` (phases list + detail block + progress table) and `.planning/REQUIREMENTS.md` (new VIS sub-section under §A).

---

## Determinism handling

| Option | Description | Selected |
|--------|-------------|----------|
| Re-baseline is ok | radius geometry change allowed; snapshots regenerated, tests updated; determinism guarantee unchanged | ✓ |
| Don't touch radius | Depth solved only via paint/StyleTemplate; synthesis `radius` untouched; snapshots stable but less geometric room | |

**User's choice:** Re-baseline is ok
**Notes:** VIS-DEPTH may change the engine contract `radius`; golden snapshots will be re-baselined.

---

## Depth geometry (VIS-DEPTH)

| Option | Description | Selected |
|--------|-------------|----------|
| Perspektive + Mindestgap | radius falls toward center but every ring keeps a guaranteed minimum gap — natural depth, no blob | ✓ |
| Linear / gleicher Abstand | Equal radial width per day; max legibility but loses depth feel, crowds core at 30+ rings | |
| Fixer Horizont (letzte N) | Last N days full-size, older compressed into an inner band; emphasizes recency | |

**User's choice:** Perspektive + Mindestgap
**Notes:** Replaces `Math.pow(0.85, idx)` in synthesis.ts.

---

## Old-shell visual weight (VIS-DEPTH)

| Option | Description | Selected |
|--------|-------------|----------|
| Verblassen + Highlights | Base brightness fades with age, but standout days (high conflict/energy) keep accents; min legibility floor | ✓ |
| Gleichmäßig verblassen | Older = uniformly dimmer/smaller, pure depth atmosphere | |
| Gleich hell & voll lesbar | All days equally present, max legibility, less depth mood | |

**User's choice:** Verblassen + Highlights

---

## Metric → motion mapping (VIS-ANIM)

| Option | Description | Selected |
|--------|-------------|----------|
| conflict→Härte, energy→Tempo | Per-metric mapping; gentle shimmer baseline instead of hard 0.10→1.0 blink; momentum may add drift | ✓ |
| Ein Intensitäts-Wert | One combined intensity scalar drives one motion parameter | |
| Richtung jetzt, Mapping im Plan | User sets direction only, exact mapping deferred to planner | |

**User's choice:** conflict→Härte/Amplitude, energy→Tempo
**Notes:** Mapping direction locked; exact curve constants left to planner.

---

## Frozen-shell character (VIS-ANIM × VIS-DEPTH)

| Option | Description | Selected |
|--------|-------------|----------|
| Ja, gebackene Varianz | Frozen days carry a static metric signature baked into the RenderTexture — zero per-frame cost | ✓ |
| Nein, nur Geometrie | Frozen shells differentiated only by radius/brightness, no metric character variance | |

**User's choice:** Ja, gebackene Varianz

---

## Claude's Discretion

- Exact radius falloff function + minimum-gap constant (behavior locked by D-01).
- Exact metric→parameter curves/constants for the ignite modulation (mapping direction locked by D-03).

## Deferred Ideas

None — discussion stayed within phase scope.
