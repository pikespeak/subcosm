---
phase: 05-submit
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/client/splash.html
  - src/client/splash.css
  - src/client/splash.ts
  - src/client/game.html
  - src/client/game.css
  - src/client/game.ts
  - src/client/cosmos/coachmark.ts
  - src/client/cosmos/coachmark.test.ts
autonomous: false
requirements: [SUB-04]

must_haves:
  truths:
    - "The in-feed splash card shows a one-line pitch hook + a static teaser visual + a clear open/play CTA — a fast static card, NOT a live mini-render."
    - "On the first run inside the game, a dismissible coachmark overlay points at: genesis core / shells / live frontier (depth = time), the goal line, the nudge/steer control, and 'it freezes & reveals overnight.'"
    - "The coachmark shows once (persists 'seen') and never reappears on subsequent opens."
    - "The coachmark respects prefers-reduced-motion (no animated reveal when reduced motion is set)."
    - "Cold-start day-1 reads as intentional Genesis (already wired) — verified, not broken/empty."
  artifacts:
    - path: "src/client/cosmos/coachmark.ts"
      provides: "One-time dismissible first-run coachmark overlay over the game canvas/HUD, persists a 'seen' flag, reduced-motion aware"
      exports: ["showCoachmarkOnce"]
      min_lines: 30
    - path: "src/client/splash.html"
      provides: "The in-feed splash card markup — hook line + static teaser + open/play CTA (D-09)"
      contains: "subcosm"
    - path: "src/client/cosmos/coachmark.test.ts"
      provides: "Tests for show-once persistence + reduced-motion gate"
  key_links:
    - from: "src/client/splash.ts"
      to: "game entrypoint"
      via: "requestExpandedMode(e, 'game') on the CTA (existing pattern, kept)"
      pattern: "requestExpandedMode"
    - from: "src/client/game.ts"
      to: "src/client/cosmos/coachmark.ts"
      via: "showCoachmarkOnce() called after the first successful universe mount"
      pattern: "showCoachmarkOnce"
  prohibitions:
    - "The splash card MUST NOT do a live mini-render (D-09) — it is a static teaser only; a heavy inline render hurts the first impression judges see (RESEARCH Q4 Pitfall)."
    - "The coachmark MUST NOT reappear after dismissal — persist the 'seen' flag client-side (D-02)."
    - "The coachmark MUST respect prefers-reduced-motion — no strobe/animated reveal under reduced motion (D-02 / PNT-04 discipline)."
    - "Do NOT echo user-authored text in the splash or coachmark — authored i18n-keyed copy only (CLAUDE.md §7)."
---

<objective>
Make the loop self-explanatory to a brand-new visitor (SUB-04) — the highest-weighted judging input is the demo link, and judging is primarily on community play. Two onboarding surfaces:

1. **In-feed splash card (D-09):** the post's default inline entrypoint (`splash.html`). Replace the starter "Hey user 👋 / Edit splash.ts" boilerplate with a reliable, fast static card: a one-line pitch hook ("a universe grown from your community"), a static teaser visual of the cosmos, and a clear open/play CTA that expands into the game. NOT a live mini-render (D-09 / RESEARCH Q4 — a heavy inline render is the first thing judges see).

2. **First-run coachmark overlay (D-02):** a one-time, dismissible overlay inside the game pointing at the genesis core / shells / live frontier (depth = time), the goal line, the nudge/steer control, and "it freezes & reveals overnight." Shown once (persist "seen"), respects `prefers-reduced-motion`, layered above the existing canvas/HUD (reuses the existing HUD chrome).

Cold-start day-1 already renders an intentional Genesis overlay (game.html `state-coldstart`, wired in 03-05) — this plan verifies it reads as intentional, it does not rebuild it.

Purpose: a new visitor understands BOTH the cosmos loop (one shell per day) and the game loop (goal → steer → overnight reveal) without external explanation (SUB-04 success criterion).
Output: a hook-first static splash card + a show-once reduced-motion-aware coachmark wired into the game mount.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-submit/05-CONTEXT.md
@.planning/phases/05-submit/05-RESEARCH.md
@CLAUDE.md
@src/client/splash.html
@src/client/splash.ts
@src/client/splash.css
@src/client/game.html
@src/client/game.css
@src/client/game.ts
@src/client/cosmos/hud.ts
@src/client/cosmos/reduced-motion.ts
@docs/subcosm.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the in-feed splash card as a static hook + teaser + CTA (D-09)</name>
  <files>src/client/splash.html, src/client/splash.css, src/client/splash.ts</files>
  <read_first>
    - src/client/splash.html + splash.ts + splash.css (the current starter boilerplate: "Hey user 👋", "Edit src/client/splash.ts", docs/r-Devvit/Discord footer — all to be replaced)
    - src/client/splash.ts (the requestExpandedMode(e, 'game') CTA pattern to KEEP — it is the open/play action)
    - .planning/phases/05-submit/05-CONTEXT.md D-09 + specifics (hook line "a universe grown from your community")
    - .planning/phases/05-submit/05-RESEARCH.md Q4 (splash is the default inline entrypoint; static teaser only, fast-loading)
    - docs/subcosm.png (the brand teaser source — logo / key art)
  </read_first>
  <action>
    Rewrite splash.html/css/ts into the D-09 card: (a) a one-line pitch hook — "a universe grown from your community" (i18n-keyed, English fallback); (b) a STATIC teaser visual of the cosmos (Claude's discretion on source — e.g. a static image asset or a lightweight CSS/SVG cosmos motif harmonizing with the Techno cyan↔magenta palette; the brand mark from docs/subcosm.png may be referenced); (c) a clear open/play CTA button that calls the EXISTING `requestExpandedMode(e, 'game')` to expand into the game. Drop the starter "Hey {username}", "Edit src/client/splash.ts", and the Docs/r-Devvit/Discord footer links. Keep it fast and reliable — NO live Phaser/WebGL mini-render in the splash (D-09 / RESEARCH Q4 Pitfall 6). Style with the wide light-sans + dark cosmos palette so the card reads as Subcosm, not the starter template. (Implements D-09.)
  </action>
  <verify>
    <automated>grep -qi 'universe grown from your community' src/client/splash.html && ! grep -qi 'Edit' src/client/splash.ts && grep -q 'requestExpandedMode' src/client/splash.ts && npm run type-check && echo PASS</automated>
  </verify>
  <done>The splash card shows the hook line + a static teaser + an open/play CTA wired to requestExpandedMode; the starter boilerplate is gone; no live render in the splash; type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: First-run coachmark overlay — show-once, reduced-motion aware (D-02)</name>
  <files>src/client/cosmos/coachmark.ts, src/client/cosmos/coachmark.test.ts, src/client/game.html, src/client/game.css</files>
  <read_first>
    - src/client/game.html (the #app / #game-container / .hud overlay structure + the existing data-i18n + [hidden]-toggle discipline to mirror; add the coachmark markup here)
    - src/client/game.css (the locked .hud chrome — translucent-dark panel + blur — to reuse for the coachmark)
    - src/client/cosmos/hud.ts (the DOM-toggle + textContent-only i18n discipline; coachmark mirrors it)
    - src/client/cosmos/reduced-motion.ts (the existing reduced-motion detection to reuse for the gate)
    - .planning/phases/05-submit/05-CONTEXT.md D-02 (the six targets + one-shot persist + prefers-reduced-motion)
  </read_first>
  <behavior>
    - showCoachmarkOnce() shows the overlay only when the persisted 'seen' flag is absent; after dismissal it sets the flag.
    - A second call to showCoachmarkOnce() (simulating a re-open) does NOT show the overlay (the flag persists).
    - Under prefers-reduced-motion the overlay still shows its content but with NO animated reveal/strobe (static appearance).
    - Dismissing the overlay (the dismiss control) hides it and persists 'seen'.
  </behavior>
  <action>
    Create `showCoachmarkOnce()` in src/client/cosmos/coachmark.ts: a one-time dismissible overlay above the game canvas/HUD that points at the six D-02 targets — genesis core / shells / live frontier (depth = time), the goal line, the nudge/steer control, and "it freezes & reveals overnight." Persist a "seen" flag client-side (Claude's discretion: localStorage or an equivalent persisted store available in the webview); on subsequent opens it no-ops. Gate any reveal animation behind the existing reduced-motion detection (reduced-motion.ts) — static appearance, no strobe (D-02 / PNT-04). Add the coachmark markup to game.html (reuse the locked `.hud` chrome from game.css; data-i18n keyed copy, English fallback, textContent-only substitution — never raw HTML, T-02-11 discipline) and any needed styles to game.css. Add a single `showCoachmarkOnce()` call in game.ts after the first successful universe mount (mountUniverse) so it overlays a populated universe (not the loading/error states). Author copy in English (CLAUDE.md). (Implements D-02.)
  </action>
  <verify>
    <automated>npm test -- src/client/cosmos/coachmark.test.ts && npm run type-check && echo PASS</automated>
  </verify>
  <done>showCoachmarkOnce shows once + persists 'seen' (re-call no-ops) + respects reduced-motion; markup uses the .hud chrome with data-i18n copy; wired into the game mount; tests + type-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify onboarding legibility (human judgment)</name>
  <action>Human checkpoint — Claude pauses here. The developer runs the build on the dev/demo sub and judges whether a first-time visitor understands both loops from the splash + coachmark alone. See how-to-verify for exact steps.</action>
  <what-built>
    The static splash card (hook + teaser + CTA) and the first-run coachmark overlay, wired into the inline post entrypoint and the game mount. SUB-04 ("a new visitor understands both loops without external explanation") is inherently a human-legibility judgment.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (devvit playtest) on the dev sub, or open the demo post once 05-02's backfill has seeded it.
    2. In-feed: confirm the splash card shows the hook line + static teaser + a clear open/play CTA, loads instantly, and is NOT a live render. Tap the CTA — it expands into the game.
    3. First open: confirm the coachmark overlay appears once and points at the genesis core, shells, the live frontier (depth = time), the goal line, the nudge control, and "freezes & reveals overnight." Dismiss it.
    4. Re-open the game: confirm the coachmark does NOT reappear.
    5. Enable reduced-motion (OS setting) and re-trigger (clear the seen flag): confirm the coachmark shows statically with no strobe/animated reveal.
    6. Judge the legibility: would a first-time visitor understand both the cosmos loop and the goal→steer→reveal loop from the splash + coachmark alone?
  </how-to-verify>
  <resume-signal>Type "approved" or describe what reads unclear / what to adjust.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host i18n copy → DOM | Onboarding copy is rendered into the webview DOM — must be authored copy, never user-echoed text or a raw-HTML sink |
| persisted 'seen' flag → client storage | The show-once flag lives in client storage; tampering only re-shows a coachmark (no security impact) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure / policy | splash + coachmark copy | mitigate | Authored i18n-keyed copy only; textContent substitution, never raw HTML (T-02-11); no user-authored content echoed (RESEARCH Q2 content policy). |
| T-05-10 | Tampering | persisted 'seen' flag | accept | Tampering only causes the coachmark to re-show; no data/security impact, low value. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). No [ASSUMED]/[SUS] packages → no install checkpoint required. |
</threat_model>

<verification>
- splash.html/ts: hook line present, boilerplate gone, CTA wired to requestExpandedMode, no live render (Task 1).
- coachmark.test.ts: show-once persistence + reduced-motion gate (Task 2).
- Human checkpoint: both loops legible from splash + coachmark (SUB-04).
- `npm test` + `npm run type-check` green (CLAUDE.md non-negotiable).
</verification>

<success_criteria>
SUB-04 satisfied: a new visitor understands both the cosmos loop and the goal→steer→reveal game loop without external explanation; the in-feed card is a fast static hook+teaser+CTA (D-09); the first-run coachmark explains the universe once, respects reduced-motion (D-02); cold-start day-1 reads as intentional Genesis (verified).
</success_criteria>

<output>
Create `.planning/phases/05-submit/05-03-SUMMARY.md` when done. Record the teaser-visual source chosen and the 'seen'-flag persistence mechanism.
</output>