---
phase: 05-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
autonomous: false
requirements: [SUB-01]
user_setup:
  - service: reddit-developer-platform
    why: "Publishing the app + requesting the public App-Directory listing are CLI→platform operations gated on a logged-in Reddit dev account and a platform review."
    env_vars: []
    dashboard_config:
      - task: "Confirm the published app's listing on the App Directory (name/slug subcosm-universe, README body, category, icon/media)"
        location: "developer.reddit.com → My Apps → subcosm-universe"

must_haves:
  truths:
    - "A published Subcosm build exists on developer.reddit.com with a working app-listing URL (Unlisted is sufficient for the link; Public is requested with buffer)."
    - "The app-listing README renders the hook-first description, not the starter-template boilerplate."
    - "The publish CLI ran against a stable 0.13.4 version (no -next prerelease)."
  artifacts:
    - path: "README.md"
      provides: "The App-Directory listing body — hook-first Subcosm description, not the Devvit-Phaser-Starter boilerplate"
      contains: "Subcosm"
    - path: "package.json"
      provides: "The deploy/launch scripts that drive devvit upload + devvit publish (verified, unchanged unless a script fix is needed)"
      contains: "launch"
  key_links:
    - from: "README.md"
      to: "developer.reddit.com app listing"
      via: "devvit publish --public uses README.md as the listing body"
      pattern: "Subcosm"
  prohibitions:
    - "Do NOT publish a -next / prerelease version (devvit publish --version rejects prereleases; unstable for judging). Stay on stable 0.13.4."
    - "Do NOT leave the public publish to the final days — publish Unlisted FIRST this wave to guarantee a valid listing link (the review SLA is unpinned)."
    - "Do NOT add new gameplay or change engine/synthesis code in this plan — it is publish + listing copy only."
---

<objective>
Retire the single deadline-critical risk FIRST: get a valid, working app-listing link on developer.reddit.com early, before the demo polish lands, so a slow public-review queue near 2026-07-15 cannot cost us SUB-01. Per RESEARCH Q1 + Pitfall 1: an **Unlisted** published app already produces a listing link that satisfies the hackathon's "link to your app on developer.reddit.com" requirement, while `devvit publish --public` (which initiates the unpinned-SLA platform review) is requested with multi-day buffer. The load-bearing artifact for a public listing is a detailed `README.md` (it becomes the listing body) — this plan replaces the starter boilerplate with the hook-first Subcosm description.

Purpose: De-risk the timeline (the dominant external risk this phase is timing, not difficulty — RESEARCH Summary). Guarantee a submittable app-listing link exists from Wave 1.
Output: A published (Unlisted) Subcosm build with a verified listing URL, a publish-ready README, and a captured decision on the Unlisted-vs-Public listing-link open question.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submit/05-CONTEXT.md
@.planning/phases/05-submit/05-RESEARCH.md
@README.md
@package.json
@devvit.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md as the hook-first app-listing body</name>
  <files>README.md</files>
  <read_first>
    - README.md (current starter boilerplate — to be replaced)
    - docs/devpost-submission.md ("Inspiration" + "What it does" — the hook narrative source, D-03)
    - .planning/PROJECT.md (brand, north-star, prize targets)
    - .planning/phases/05-submit/05-RESEARCH.md Q1 (README is the listing body; must be detailed for --public)
  </read_first>
  <action>
    Replace the "Devvit Phaser Starter" boilerplate in README.md with the Subcosm app-listing body. Lead with the emotional hook per D-03: "a universe grown from your community" / "what did our community become overnight?" — then substantiate with what it does (one shared cosmos per subreddit, depth = time, daily goal → steer → overnight reveal) and the architecture one-liner (one pure deterministic engine, two typed contracts, no stored images, ~25 scalars + seed per ring). Keep the existing "Commands" section (deploy/launch/dev/login are accurate per package.json) but drop the "create devvit" install wizard steps and the Phaser-template credit framing that reads as boilerplate. Write in English (CLAUDE.md). Do NOT echo any user-authored content; this is authored marketing copy. Keep it detailed enough to satisfy the `--public` listing requirement (RESEARCH Q1: a stub README can bounce the public listing). Do NOT claim "Best Use of Phaser" as a target (D-04 deferred it) — Phaser is described factually as the paint layer only.
  </action>
  <verify>
    <automated>grep -qi 'universe grown from your community' README.md && grep -qi 'subcosm' README.md && ! grep -qi 'Devvit Phaser Starter' README.md && echo PASS</automated>
  </verify>
  <done>README.md leads with the Subcosm hook, describes the cosmos + game loop + determinism, retains accurate command docs, and no longer contains the starter-template boilerplate heading.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the publish toolchain is green and on stable 0.13.4 (no publish yet)</name>
  <files>package.json</files>
  <read_first>
    - package.json (scripts: deploy = type-check + lint + devvit upload; launch = deploy + devvit publish)
    - .planning/phases/05-submit/05-RESEARCH.md Q1 + "Standard Stack" (devvit 0.13.4 latest; prereleases rejected by --version)
  </read_first>
  <action>
    Confirm the build + publish chain is ready so the human publish step (Task 3) cannot fail on a stale build. Run the existing `npm run type-check`, `npm run lint`, and `npm test` and confirm all green (CLAUDE.md non-negotiable: build + tests always pass). Verify the installed devvit CLI version is the stable `0.13.4` (per RESEARCH Standard Stack) and NOT a `-next` prerelease. Do NOT run `devvit upload`/`devvit publish` here (that is the human checkpoint in Task 3, which needs a logged-in account). If a `package.json` script needs a fix to make the chain green (e.g. a broken script reference), make the minimal fix and note it; otherwise leave package.json unchanged.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm test && npx devvit --version | grep -E '^0\.13\.4' && echo PASS</automated>
  </verify>
  <done>type-check, lint, and the full vitest suite are green; the devvit CLI reports a stable 0.13.4 version (no -next suffix); the deploy/launch chain is ready for a human publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Publish Unlisted + verify the app-listing link (human/platform-gated)</name>
  <action>Human checkpoint — Claude pauses here. The developer runs the publish CLI (Unlisted first), confirms the app-listing link, resolves the Unlisted-vs-Public open question, and optionally requests --public with buffer. See how-to-verify for exact steps.</action>
  <what-built>
    A publish-ready README (the App-Directory listing body) and a verified-green build chain. The actual publish is a CLI→platform operation that requires the logged-in Reddit dev account (u/Aware_Picture1973) — Claude cannot perform it.
  </what-built>
  <how-to-verify>
    1. Ensure you are logged in: `npm run login` (devvit login) if needed.
    2. Publish UNLISTED first (de-risk the timeline — RESEARCH Pitfall 1): run `npm run deploy` (type-check + lint + devvit upload), then `devvit publish` and choose **Unlisted** when prompted for visibility (do NOT choose a -next/prerelease version; use a stable version per RESEARCH Q1).
    3. Confirm the App Directory shows the listing: developer.reddit.com → My Apps → subcosm-universe. Copy the listing URL and paste it into the SUMMARY.
    4. Confirm the listing URL opens in a browser and shows the README body (not the starter boilerplate).
    5. RESOLVE THE OPEN QUESTION (RESEARCH Open Q1 / Assumption A2): check the Devpost rules / r/devvit whether the hackathon accepts an Unlisted app's listing link or requires a Public-directory listing. Record the answer in the SUMMARY.
    6. If Public is required (or to maximize standing), request the public listing with buffer: `devvit publish --public --version X.Y.Z` (stable version; README must be detailed — Task 1 ensured this). Note: this enters the platform review with an UNPINNED SLA — that is why it is requested early. Record the review status (pending/approved) in the SUMMARY; a still-pending public review does NOT block submission as long as the Unlisted link is valid.
    7. Re-publishing after later waves (final polished build) is expected — `devvit upload` is repeatable; this checkpoint only needs to prove a VALID LISTING LINK exists now.
  </how-to-verify>
  <resume-signal>Type "approved" with the listing URL + the Unlisted-vs-Public answer, or describe what blocked the publish.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Reddit platform | `devvit upload`/`publish` push a build + listing metadata to Reddit; the platform review gate is outside our control |
| README content → public listing | README.md becomes the public-facing listing body — must contain no prohibited/echoed content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure / policy | README listing body | mitigate | README is authored marketing copy only — no user-authored content echoed, no secrets/keys, no prohibited content (Devvit rules, RESEARCH Q2). |
| T-05-02 | Denial of Service (timeline) | public-review queue near deadline | mitigate | Publish Unlisted FIRST (Wave 1) to guarantee a valid link; request `--public` with multi-day buffer (RESEARCH Pitfall 1 / A1). |
| T-05-03 | Tampering | publish version | mitigate | Publish a stable 0.13.4 version only; never a `-next` prerelease (rejected by `--version`, unstable for judging — RESEARCH Q1). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). No [ASSUMED]/[SUS] packages → no install checkpoint required. |
</threat_model>

<verification>
- README.md leads with the Subcosm hook and contains no starter boilerplate (Task 1 grep).
- `npm run type-check`, `npm run lint`, `npm test` all green (Task 2).
- devvit CLI is stable 0.13.4 (Task 2).
- A valid app-listing URL exists and renders the README body (human checkpoint).
- The Unlisted-vs-Public listing-link open question is resolved and recorded (human checkpoint).
</verification>

<success_criteria>
SUB-01 satisfied: the app is published with a working app-listing link on developer.reddit.com (Unlisted guarantees the link; Public requested with buffer). The listing body is the hook-first Subcosm README. The publish-review timeline risk is retired in Wave 1.
</success_criteria>

<output>
Create `.planning/phases/05-submit/05-01-SUMMARY.md` when done. MUST record: the app-listing URL, the publish visibility chosen (Unlisted/Public), the public-review status if requested, and the resolved Unlisted-vs-Public open question.
</output>
