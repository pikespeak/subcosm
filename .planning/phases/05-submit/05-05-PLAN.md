---
phase: 05-submit
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - docs/devpost-submission.md
  - .planning/phases/04-live-game/04-UAT.md
autonomous: false
requirements: [SUB-02, SUB-05, SUB-06]

must_haves:
  truths:
    - "ONE on-device validation pass runs on the public demo subreddit using the backfill seed (D-01) + a forced tick (D-08): reveal-post ~1min timing, persistent reward glyph, two-client/mobile frozen-render parity, and realtime nudge propagation (or the D-03b reload fallback) are all confirmed."
    - "A public demo post on a live subreddit runs the game, is self-explanatory and playable (judged primarily on this)."
    - "docs/devpost-submission.md is complete: every [TODO] filled — app-listing link, demo-post link, media gallery (captured from the demo session), prize-category tags (Best App with a Hook + Best User Contributions)."
    - "The look + content are confirmed Devvit-rules compliant (deterministic geometry, no echoed user text) on the live post."
    - "The deferred Phase-4 UAT (04-UAT.md) is re-opened, resolved (pass/issue), and its status updated."
  artifacts:
    - path: "docs/devpost-submission.md"
      provides: "The completed Devpost write-up — hook-first narrative (D-03), filled links + media + category tags (D-04), no remaining [TODO]"
      contains: "developer.reddit.com"
    - path: ".planning/phases/04-live-game/04-UAT.md"
      provides: "The folded Phase-4 UAT re-opened and resolved (the four deferred items marked pass/issue with on-device evidence)"
      contains: "Phase 5"
  key_links:
    - from: "docs/devpost-submission.md"
      to: "the published app listing + demo post"
      via: "the 'Try it out' links filled from 05-01's listing URL + this plan's demo-post URL"
      pattern: "developer\\.reddit\\.com"
    - from: "05-02 demo control (backfill + force-tick)"
      to: "the on-device UAT pass"
      via: "the UAT uses the seed action + the force-tick action to exercise tick->reveal->glyph->cross-client on the live sub"
      pattern: "force-tick"
  prohibitions:
    - "Do NOT mark the UAT items pass without an actual on-device run on a real subreddit (D-06 — these need the live Devvit runtime; Playwright-on-bundle cannot exercise it)."
    - "Do NOT leave any [TODO] in docs/devpost-submission.md at completion (SUB-06)."
    - "Do NOT target Best Use of Phaser in the write-up framing (D-04 deferred it) — target Best App with a Hook (primary) + Best User Contributions."
    - "Do NOT echo user-authored text in any media/screenshot caption or the post (SUB-05 compliance)."
---

<objective>
The submission-completing plan. Three human/platform-gated deliverables that can only be done once the app is published (05-01), the demo tooling exists (05-02), and the build is onboarding+polished (05-03, 05-04):

1. **Folded on-device UAT (D-06, SUB-02):** run the deferred Phase-4 UAT (.planning/phases/04-live-game/04-UAT.md — reveal-post ~1min timing, persistent reward glyph, two-client/mobile frozen-render parity, realtime nudge propagation / D-03b fallback) in ONE on-device pass on the public demo subreddit, using the backfill seed (D-01) + a forced tick (D-08). Also profile ~60fps on the real mid-range Android (the D-05 on-device confirmation deferred from 05-04). Re-open + resolve 04-UAT.md.

2. **Public demo post (SUB-02):** create the canonical pinned public demo post on a real subreddit you moderate, seeded with the 30-day arc, self-explanatory and playable — this is what judging is primarily based on.

3. **Devpost write-up (SUB-06):** complete docs/devpost-submission.md — fill every [TODO] (app-listing link from 05-01, demo-post link, media gallery captured from the demo session, prize-category tags Best App with a Hook + Best User Contributions per D-04), keeping the hook-first narrative (D-03). Confirm Devvit-rules compliance on the live post (SUB-05).

Purpose: every mandatory hackathon artifact exists, validated on a real device, before 2026-07-15 18:00 PDT.
Output: a resolved UAT, a live public demo post, and a complete Devpost submission.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-submit/05-CONTEXT.md
@.planning/phases/05-submit/05-RESEARCH.md
@.planning/phases/04-live-game/04-UAT.md
@docs/devpost-submission.md
@CLAUDE.md
@src/server/core/post.ts
@devvit.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: On-device UAT pass + create the public demo post (human/platform-gated)</name>
  <action>Human checkpoint — Claude pauses here. The developer deploys to a real demo sub, seeds + force-ticks via the mod menu, creates the pinned public demo post, runs the four folded UAT items + on-device fps on a real Android, confirms compliance, and captures media. See how-to-verify for exact steps.</action>
  <what-built>
    The full submission-ready stack: published app (05-01), demo control (backfill + force-tick, 05-02), onboarding (05-03), perf + aesthetic (05-04). This checkpoint creates the public demo post and runs the ONE folded on-device UAT pass (D-06) on a real subreddit with a real mid-range Android — the live Devvit runtime that unit tests cannot exercise.
  </what-built>
  <how-to-verify>
    PREREQUISITE: a public subreddit you moderate with the app installed (RESEARCH Environment Availability: only the dev sub is confirmed — create/own a public sub for the submission demo). A real mid-range Android (D-05/D-06).
    1. Deploy the final polished build to the demo sub: `npm run deploy` (and re-publish per 05-01 if this is the final build).
    2. SEED: run the mod menu "Seed demo history" (05-02 backfill) — confirm the universe opens onto a deep, populated 30-day cosmos (not empty day-1), rings byte-identical to organic, and NO spam of ~30 pinned posts appeared.
    3. CREATE the public demo post (the existing "Create a new post" mod action) and pin it as the canonical demo post. Copy its URL for the SUMMARY + Devpost.
    4. FORCE TICK: run "Advance day / trigger tick" (05-02 force-tick). Confirm the four folded 04-UAT items:
       a. A pinned reveal post appears within ~1 min, stating the day's goal + achieved ✓/✗ (exactly one post — revealDone nx-guard).
       b. Scrub to the just-frozen achieved ring → the persistent reward glyph is visible.
       c. Load the post on a SECOND client (incl. mobile) → the frozen ring renders identically (Redis→engine determinism).
       d. Nudge from one client → it propagates to a second viewer in near-real-time; if not, confirm the D-03b reload-reconciliation fallback keeps clients consistent on reload. RECORD which path was in effect.
    5. PERF (D-05/SUB-03 on-device): on the real mid-range Android, confirm ~60fps in the post viewport, no jank on scrub/nudge. Record observed fps/feel.
    6. COMPLIANCE (SUB-05): confirm the live post renders deterministic geometry only and echoes NO user-authored text.
    7. CAPTURE MEDIA: screenshots/GIFs from this session for the Devpost gallery (genesis core, a dense vs quiet shell, the overnight reveal, the depth scrubber, two genome presets) — JPG/PNG/GIF, the gallery constraints noted in the draft (confirm the form's exact limits).
    8. Update .planning/phases/04-live-game/04-UAT.md: re-open the four deferred items and mark each pass/issue with the evidence from this run.
  </how-to-verify>
  <resume-signal>Type "approved" with the demo-post URL + the realtime-path-in-effect + observed fps, or describe any UAT issue found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update 04-UAT.md — re-open + resolve the four deferred items</name>
  <files>.planning/phases/04-live-game/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-live-game/04-UAT.md (the four deferred items + the deferral decision frontmatter)
  </read_first>
  <action>
    Update 04-UAT.md from the on-device run (checkpoint above): change the four items from "[deferred → Phase 5 demo]" to their resolved result (pass / issue, with the on-device evidence — reveal timing observed, glyph confirmed, cross-client parity confirmed, realtime path in effect). Update the frontmatter status from `deferred` to `resolved` (or `complete`) and the Summary counts (passed/issues). If any item is an ISSUE, record it clearly so a follow-up can address it before the deadline. Do NOT mark anything pass without the actual on-device evidence (D-06).
  </action>
  <verify>
    <automated>grep -qi 'Phase 5' .planning/phases/04-live-game/04-UAT.md && ! grep -q 'deferred → Phase 5 demo' .planning/phases/04-live-game/04-UAT.md && echo PASS</automated>
  </verify>
  <done>04-UAT.md's four items are resolved (pass/issue) with on-device evidence; the deferral status is cleared; the Summary counts are updated.</done>
</task>

<task type="auto">
  <name>Task 3: Complete docs/devpost-submission.md — fill every [TODO] (SUB-06)</name>
  <files>docs/devpost-submission.md</files>
  <read_first>
    - docs/devpost-submission.md (the draft + every [TODO] marker: build narrative/numbers, Try-it-out links, media gallery, checklist)
    - .planning/phases/05-submit/05-CONTEXT.md D-03 (hook-first narrative) + D-04 (Best App with a Hook PRIMARY + Best User Contributions; NOT Best Use of Phaser)
    - .planning/phases/05-submit/05-RESEARCH.md Q3 (Devpost required fields, deadline, judging, media-gallery constraints)
    - the 05-01 SUMMARY (the app-listing URL) + this plan's checkpoint result (the demo-post URL + media + on-device numbers)
  </read_first>
  <action>
    Fill EVERY [TODO] in docs/devpost-submission.md: (a) the "Try it out" links — the app-listing URL (from 05-01) + the public demo-post URL (from the checkpoint); (b) the build-narrative real numbers ([TODO] in "How we built it") — measured on-device fps, ring count, draw-call/perf notes from the UAT run; (c) the media gallery — the screenshots/GIFs captured in the checkpoint (genesis core, dense vs quiet shell, overnight reveal, depth scrubber, two genome presets), respecting the gallery constraints; (d) tick the pre-submission checklist as items are confirmed. Keep the hook-first narrative (D-03). Ensure the prize-category framing targets Best App with a Hook (primary) + Best User Contributions (D-04) — do NOT frame for Best Use of Phaser. Confirm the deadline (2026-07-15 18:00 PDT) is correct. No remaining [TODO] markers. English (CLAUDE.md). Note: the architecture math in the draft references the OLD radius falloff `0.85^i` — reconcile it with the current clamped-min-gap geometry (02.1) if you cite formulas, or describe it qualitatively to avoid an inaccurate claim.
  </action>
  <verify>
    <automated>! grep -q '\[TODO\]' docs/devpost-submission.md && grep -q 'developer.reddit.com' docs/devpost-submission.md && grep -qi 'Best App with a Hook' docs/devpost-submission.md && echo PASS</automated>
  </verify>
  <done>docs/devpost-submission.md has zero [TODO] markers; the Try-it-out links + media gallery + real numbers are filled; the narrative is hook-first; the category framing is Best App with a Hook + Best User Contributions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Submit the Devpost entry before the deadline (off-platform human action)</name>
  <action>Human checkpoint — Claude pauses here. The developer reviews the completed write-up, fills the Devpost form (links + media + categories), and clicks Submit before 2026-07-15 18:00 PDT. See how-to-verify for exact steps.</action>
  <what-built>
    The completed Devpost write-up + the confirmed live demo post + the resolved UAT. The actual Devpost form submission (pasting links, uploading the gallery, selecting categories, clicking Submit before the deadline) is an off-platform human action.
  </what-built>
  <how-to-verify>
    1. Review docs/devpost-submission.md end-to-end — confirm no [TODO] remains, links open, the narrative leads with the hook, the gallery images are attached/uploaded.
    2. On redditgameswithahook.devpost.com: create/edit the submission — paste the app-listing link + demo-post link, upload the media gallery, select the prize categories (Best App with a Hook + Best User Contributions), (optionally) the Developer Platform feedback survey for Feedback Award eligibility.
    3. Confirm both required fields are present (app listing + demo post) and the submission validates.
    4. SUBMIT before 2026-07-15 18:00 PDT. Confirm the submission shows as submitted.
    5. (If the public app-listing review from 05-01 is still pending, confirm the Unlisted link is valid and accepted — per the open question resolved in 05-01.)
  </how-to-verify>
  <resume-signal>Type "submitted" with the Devpost submission URL, or describe what blocked submission.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mod demo actions → live community | Seeding + force-tick mutate the public demo sub's real state (rings + a pinned reveal post) |
| live post → judges/public | The public post renders to anyone; it must echo no user-authored content (Devvit content policy) |
| Devpost form → public submission | Links + media are submitted publicly; no secrets/credentials/echoed user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Information Disclosure / policy | live demo post + media captions | mitigate | The post renders deterministic geometry only; media/captions are authored, no user-authored text echoed (SUB-05 / RESEARCH Q2 — confirmed on-device in the checkpoint). |
| T-05-15 | Tampering / policy (post spam) | seeding the demo sub | mitigate | Backfill (05-02) writes rings only — no per-day reveal posts; the demo sub gets one canonical demo post + one forced reveal, not spam (RESEARCH Pitfall 2). |
| T-05-16 | Denial of Service (timeline) | Devpost submission near deadline | mitigate | The app-listing link is already secured (05-01 Wave 1); this plan only needs the demo-post link + media; submit with buffer before 2026-07-15 18:00 PDT. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). No [ASSUMED]/[SUS] packages → no install checkpoint required. |
</threat_model>

<verification>
- On-device UAT pass run on the real demo sub + Android (checkpoint 1): reveal timing, glyph, cross-client parity, realtime/fallback, fps, compliance.
- 04-UAT.md re-opened + resolved with evidence (Task 1).
- docs/devpost-submission.md has zero [TODO], links + media + categories filled (Task 2).
- Devpost submission submitted before the deadline (checkpoint 2).
</verification>

<success_criteria>
SUB-02 fully satisfied: a public, self-explanatory, playable, populated demo post on a real subreddit, validated on-device (the four folded UAT items resolved). SUB-05 confirmed on the live post (deterministic geometry, no echoed text). SUB-06 satisfied: docs/devpost-submission.md complete with links + media + category tags, submitted before 2026-07-15 18:00 PDT.
</success_criteria>

<output>
Create `.planning/phases/05-submit/05-05-SUMMARY.md` when done. Record: the demo-post URL, the realtime-path in effect (realtime vs D-03b fallback), observed on-device fps, the four UAT item results, and the Devpost submission URL/status.
</output>
