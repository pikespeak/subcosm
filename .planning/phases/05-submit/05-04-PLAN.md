---
phase: 05-submit
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - src/client/game.ts
  - src/client/cosmos/paint.ts
  - src/styles/techno.ts
  - src/client/game.css
autonomous: false
requirements: [SUB-03, SUB-05]

must_haves:
  truths:
    - "The frontier animation + rAF loop hold approximately 60fps in the post viewport on a mid-range Android, with no visible jank during scrub or nudge."
    - "The rAF loop pauses/idles when the document is hidden (visibilitychange) and DPR is capped (<=2)."
    - "The first impression is a static frame that looks good before any user interaction (designed around the iOS cross-origin-iframe 30fps-until-interaction throttle)."
    - "The genesis core carries the bespoke long cross-flare from docs/subcosm.png; the palette is the curated cyan<->magenta signature; HUD/splash use wide light-sans typography; the SUBCOSM logo reads as a brand mark."
    - "The look reads self-authored, not generic-neon-fractal AI-slop; geometry stays deterministic; no user text is echoed (Devvit-rules compliant)."
  artifacts:
    - path: "src/client/cosmos/paint.ts"
      provides: "Sharpened genesis-core cross-flare + the perf guards (frame-skip/DPR/visibility) on the live frontier paint"
      contains: "flare"
    - path: "src/styles/techno.ts"
      provides: "The curated cyan<->magenta palette + line/glow constants as DATA (PNT-02 — look is data, not code)"
      contains: "ramp"
    - path: "src/client/game.ts"
      provides: "rAF/visibility + DPR perf guards on the post webview mount"
      contains: "visibilitychange"
  key_links:
    - from: "src/styles/techno.ts"
      to: "src/client/cosmos/paint.ts"
      via: "paint reads palette/line/fill constants from the StyleTemplate, never hard-codes them (ENG-02/PNT-02)"
      pattern: "style\\.(palette|line|fill)"
    - from: "src/client/game.ts"
      to: "document visibilitychange"
      via: "rAF loop idles when hidden to bound per-frame cost"
      pattern: "visibilitychange"
  prohibitions:
    - "Do NOT re-architect the renderer — D-05 is profiling + tuning, not new architecture (only the frontier animates; frozen shells stay baked; LOD by zoom — already built; RESEARCH Q7)."
    - "Do NOT uncap rAF to chase a number above 60 — target a stable 60 (graceful 30); the webview caps at 60 (RESEARCH Q7)."
    - "Do NOT move any look constant into the painter as a hard-coded literal — look stays DATA in the StyleTemplate (PNT-02)."
    - "Do NOT echo user-authored text into the paint/post; geometry is deterministic only (SUB-05 / Devvit-rules compliance)."
    - "Do NOT add the fbm/WebGL shader layer or push Best-Use-of-Phaser (deferred, out of scope)."
---

<objective>
Two finish-polish surfaces on the already-built renderer:

1. **Mobile perf (D-05, SUB-03):** profile + tune the frontier animation + rAF loop to ~60fps in the post viewport on a mid-range Android, with no jank on scrub/nudge. The architecture is correct (only the frontier animates; frozen shells bake to RenderTexture; LOD by zoom; DPR cap 2 already in game.ts) — this is tuning, NOT a rebuild (RESEARCH Q7). Add the remaining guards: a frame-skip to a stable target frame duration, an explicit rAF idle on `visibilitychange` (the browser throttles hidden iframes anyway; explicit pause avoids wasted work), and confirm the DPR cap. Design around the iOS cross-origin-iframe reality: rAF is throttled to ~30fps until the user interacts, so the first impression must look good STATIC (the D-09 splash + a good static first frame) — never lead with an autoplay intro animation that iOS throttles to jank (RESEARCH Pitfall 5).

2. **Bespoke Techno aesthetic (D-07, SUB-05):** sharpen the self-authored signature so it does not read as generic neon-fractal AI-slop: the genesis-core long cross-flare from docs/subcosm.png, the curated cyan↔magenta palette (kept as StyleTemplate DATA — PNT-02), wide light-sans typography in HUD/splash, and the SUBCOSM logo as a brand mark. Compliance is a verification check: deterministic geometry only, no echoed user text (SUB-05 / Devvit rules).

Purpose: SUB-03 (60fps mobile, no jank) + SUB-05 (look reads self-authored + Devvit-compliant). On-device profiling (D-05) is validated in the folded UAT (05-05); this plan delivers the tuned build + the aesthetic, then 05-05 confirms on a real device.
Output: perf guards on the live frontier paint + mount; a sharpened genesis-core flare + curated palette/typography; all look constants stay as data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-submit/05-CONTEXT.md
@.planning/phases/05-submit/05-RESEARCH.md
@CLAUDE.md
@src/client/game.ts
@src/client/cosmos/paint.ts
@src/styles/techno.ts
@src/client/cosmos/ignite.ts
@src/client/cosmos/reduced-motion.ts
@src/client/game.css
@docs/subcosm.png
@docs/subcosm-universe-mock.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: rAF/visibility + frame-skip perf guards on the post webview mount (D-05)</name>
  <files>src/client/game.ts, src/client/cosmos/paint.ts</files>
  <read_first>
    - src/client/game.ts (gameConfig: the existing DPR cap min(devicePixelRatio,2); the mount/teardown lifecycle — add the visibility guard here without leaking listeners across re-mounts)
    - src/client/cosmos/paint.ts (the live frontier paint + the rAF-driven ignite pulse — the per-frame cost surface)
    - src/client/cosmos/ignite.ts (igniteParams — the data-driven pulse; the frame the loop animates)
    - src/client/cosmos/reduced-motion.ts (the existing static-frame gate to keep intact)
    - .planning/phases/05-submit/05-RESEARCH.md Q7 (frame-skip to ~16.67ms target; pause rAF on visibilitychange; DPR cap; webview 60fps ceiling; iOS 30fps-until-interaction)
  </read_first>
  <action>
    Add the remaining mobile-perf guards WITHOUT re-architecting (D-05 is tuning — RESEARCH Q7). (a) Confirm/keep the DPR cap at <=2 in gameConfig. (b) Add a frame-skip guard so the frontier animation advances on a stable target frame duration (~16.67ms for 60) and skips when delta < target — consistent speed across refresh rates, bounded per-frame cost (RESEARCH Q7). (c) Idle the rAF/animation loop when the document is hidden via a `visibilitychange` listener, and resume on visible — register it on mount, remove it in teardown() so re-mounts never leak listeners (mirror the existing teardown discipline). (d) Ensure the first rendered frame is a good STATIC frame (no autoplay intro the iOS iframe throttles to jank — RESEARCH Pitfall 5); the live ignite pulse is fine once visible/interacted. Keep the reduced-motion static gate intact (PNT-04). Do NOT uncap rAF or chase >60. (Implements D-05.)
  </action>
  <verify>
    <automated>grep -q 'visibilitychange' src/client/game.ts && grep -q 'devicePixelRatio' src/client/game.ts && npm run type-check && echo PASS</automated>
  </verify>
  <done>The mount has a DPR cap (<=2), a frame-skip guard for a stable target frame duration, and an rAF idle on visibilitychange that is removed in teardown; the first frame is static; reduced-motion gate intact; type-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Sharpen the bespoke Techno signature — genesis-core cross-flare + curated palette + typography (D-07)</name>
  <files>src/client/cosmos/paint.ts, src/styles/techno.ts, src/client/game.css</files>
  <read_first>
    - src/styles/techno.ts (the palette.ramp + line/fill/texture constants — look is DATA here, PNT-02; tune the cyan<->magenta ramp here, not in paint)
    - src/client/cosmos/paint.ts (drawGenesisCore — the core glow/dot at l.291-304; add the long cross-flare here, reading colors from the style ramp, no hard-coded literal)
    - src/client/game.css (the .hud chrome typography — apply the wide light-sans treatment)
    - docs/subcosm.png (the brand anchor: genesis-core long cross-flare, cyan<->magenta nebula, wide light-sans SUBCOSM)
    - docs/subcosm-universe-mock.html (the authored Techno reference to judge parity against)
    - .planning/phases/05-submit/05-CONTEXT.md D-07 + specifics
  </read_first>
  <action>
    Sharpen the self-authored Techno signature (D-07): (a) In paint.ts drawGenesisCore, add the long cross-flare from docs/subcosm.png — a deterministic additive cross/star-flare on the genesis core, reading its colors from the style ramp (coreStop / hueToColor), NEVER a hard-coded color literal in the painter (PNT-02). (b) In techno.ts, curate the cyan↔magenta palette ramp so it reads as the bespoke signature (kept as DATA — paint consumes it). (c) In game.css, apply wide light-sans typography to the HUD/state-overlay chrome so the type reads as the SUBCOSM brand. (d) Integrate the SUBCOSM logo as a brand mark where appropriate (Claude's discretion — e.g. a corner mark in the game chrome or reuse on the splash from 05-03). Keep ALL geometry deterministic (no rng added to paint — ENG-02/SUB-05) and echo NO user text. Re-judge side-by-side vs docs/subcosm-universe-mock.html in the checkpoint. (Implements D-07.)
  </action>
  <verify>
    <automated>grep -qi 'flare' src/client/cosmos/paint.ts && npm test -- src/client/cosmos/ignite.test.ts && npm run type-check && npm run lint && echo PASS</automated>
  </verify>
  <done>The genesis core has a deterministic long cross-flare reading colors from the style ramp; the curated cyan<->magenta palette lives in techno.ts as data; HUD/chrome use wide light-sans; no hard-coded color literals added to the painter; no rng added to paint; tests + type-check + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify perf feel + bespoke aesthetic (human judgment)</name>
  <action>Human checkpoint — Claude pauses here. The developer runs the dev harness / playtest, judges scrub/nudge smoothness + the bespoke aesthetic side-by-side vs the mock, and confirms Devvit-rules compliance. See how-to-verify for exact steps.</action>
  <what-built>
    The perf-tuned build (frame-skip + visibility idle + DPR cap) and the sharpened bespoke Techno aesthetic (genesis-core cross-flare, curated cyan<->magenta palette, wide light-sans typography, SUBCOSM brand mark). SUB-03 (60fps mobile feel) and SUB-05 (look reads self-authored) are inherently human/device judgments — full on-device fps profiling happens in the folded UAT (05-05); this checkpoint is the desktop/dev-harness sanity + aesthetic judgment.
  </what-built>
  <how-to-verify>
    1. Run `npm run cosmos` (dev harness) and/or `npm run dev` (devvit playtest) and scrub through the universe + nudge the frontier — confirm no visible jank, smooth animation, the static first frame looks intentional.
    2. Judge the aesthetic side-by-side vs docs/subcosm-universe-mock.html and docs/subcosm.png: does the genesis-core long cross-flare read like the brand? Is the cyan<->magenta palette the bespoke signature (not generic neon)? Does the typography read as SUBCOSM? Does the whole look read self-authored rather than AI-slop?
    3. Confirm Devvit-rules compliance: only deterministic geometry renders; NO user-authored text is echoed anywhere in the post/paint (SUB-05).
    4. (Deep on-device 60fps profiling on a real mid-range Android is the folded UAT in 05-05 — note any concerns here to verify there.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe the jank / aesthetic adjustments needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene data → paint | Paint reads only Scene-derived values + the StyleTemplate; raw DayVector/user text never reaches paint (ENG-02) |
| rendered post → judges | The post renders deterministic geometry only; it must echo no user-authored content (Devvit content policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure / policy | paint output | mitigate | Paint renders deterministic geometry only from the Scene + StyleTemplate; no user-authored text echoed (SUB-05 / RESEARCH Q2). |
| T-05-12 | Tampering (determinism) | paint flare | mitigate | The cross-flare is a pure function of the Scene + style ramp — no rng added to paint (ENG-02/LIVE-03 determinism); identical on every client. |
| T-05-13 | Denial of Service (perf) | rAF loop on hidden/throttled iframe | mitigate | Frame-skip guard + visibilitychange idle + DPR cap bound per-frame cost; loop pauses when hidden (RESEARCH Q7). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). No [ASSUMED]/[SUS] packages → no install checkpoint required. |
</threat_model>

<verification>
- game.ts: visibilitychange idle + DPR cap present; frame-skip guard (Task 1).
- paint.ts: deterministic genesis-core cross-flare reading style ramp; no hard-coded color literal; no rng (Task 2).
- techno.ts: curated cyan<->magenta palette as data (Task 2).
- Human checkpoint: no jank + look reads self-authored + Devvit-compliant (SUB-03/SUB-05).
- `npm test` + `npm run type-check` + `npm run lint` green (CLAUDE.md non-negotiable).
</verification>

<success_criteria>
SUB-03 satisfied (target): ~60fps in the post viewport, no scrub/nudge jank, perf guards in place, first impression static (deep on-device confirmation in 05-05). SUB-05 satisfied: the bespoke Techno signature reads self-authored (genesis-core cross-flare, curated palette, wide light-sans, brand mark); geometry deterministic; no user text echoed (Devvit-compliant).
</success_criteria>

<output>
Create `.planning/phases/05-submit/05-04-SUMMARY.md` when done. Record the perf guards added, the palette tuning, and any [ASSUMED] tuning constants for on-device confirmation in 05-05.
</output>
