// src/sim — the deterministic activity simulator (the ONE Zod boundary that emits
// DayVector[] for Phase 2). Implemented in plan 02-02; this barrel exists now so
// the `tools/tsconfig.sim.json` project + the Phaser-ban ESLint block have a
// concrete file to cover from plan 02-01 (build-surface bootstrap).
export {};
